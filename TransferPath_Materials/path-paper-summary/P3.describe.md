
| 引用标识 | 标题 | 关键思想与方法 | P3 归属判定 | 产物形态 | 领域 | 备注 |
|---|---|---|---|---|---|---|
| [Che25b] | Log-Augmented Generation: Scaling Test-Time Reasoning with Reusable Computation | 论文复用的是过往求解过程中 agent 自生成的 reasoning logs，来源是先前任务执行留下的推理与动作轨迹。方法先对整段历史 log 做完整 forward pass，再只为被选中的少量 token 持久化真实 `KV cache`，论文重点比较了存最后一轮 response 或最后一个 action 等选择策略；底座 Llama 保持冻结，检索只用外部 embedding model，不训练 backbone。latent 可离线批量预建成静态 log store，也可在新一次执行后把新 log 的 KV 继续写回。下游使用时先按语义相似度检索相关 log，再把取回的历史 KV 做 RoPE 去位置化与重定位，作为 `past_key_values` 直接注入当前生成；写入是选 token 后存 KV，读取是 top-k log retrieval，文中评测设置基本采用静态库，未强调复杂淘汰机制。 | 属于 P3 | 从完整历史推理 log 计算并持久化的真实 `past_key_values`，经检索和位置重映射后直接送入解码器注意力。 | reasoning | 检验一通过：被复用的是历史 KV，本体直接进入 attention，不是只把 embedding 当检索索引。检验二通过：KV 来自先前任务日志并在新任务中跨样本复用，目标是复用历史 reasoning computation，不是单次推理内的 cache 加速。其独特点是只保存少量 token 的 KV 但让这些 KV 代表整段历史上下文；代价是依赖位置重映射与 token 选择策略。 |
| [Sun26] | TempoFit: Plug-and-Play Layer-Wise Temporal KV Memory for Long-Horizon Vision-Language-Action Manipulation | 论文复用的是 VLA 在早先时间步上产生的 prefix attention K/V，来源是同一长程操作 episode 内 agent 自身的视觉与语言编码过程。方法不训练新的压缩器，而是在冻结 backbone 的条件下，直接缓存各层 prefix-time projection 得到的真实 K/V，并以层级 FIFO buffer 持久保存。latent 在 inference 时在线产生并立即写入缓存，随后通过当前 key 与历史 key 的点积相似度做 K-to-K retrieval，并加上 frame-gap temporal bias 控制时间偏置。被读出的历史 K/V 以 pre-attention residual loading 的方式加回当前层 KV，再由原始 self-attention 消费；写入是每步 append，读取是 top 相似历史，淘汰是固定容量 FIFO。 | 属于 P3 | 同一操作轨迹内逐步积累的层级 prefix K/V cache，经检索后残差式并入当前层 KV，再由冻结 VLA 的 self-attention 直接消费。 | embodied / robotics | 检验一通过：历史 KV 不仅参与检索，还被残差并入当前 KV，随后直接进入 attention 计算。检验二通过：latent 承担长程 manipulation episode 内的历史经验复用语义，解决 non-Markov 决策，而非 serving 级 cache 省算。需要注明的是，它主要是同一 episode 内记忆，没有论文层面的跨 session 持久化。 |
| [Wu22] | Memorizing Transformers | 论文处理的是普通语言建模序列中的过去 token 表征，来源是同一 document 或同一长序列 earlier subsequences 的内部激活，而非 agent 交互日志。方法把标准 Transformer forward pass 产生的真实 key value 对追加到外部 memory 中，并在后续位置用近邻检索读回；模型本体是训练态，既可从头训练也可 finetune，融合时还有可学习 gate。latent 在序列处理过程中在线产生并直接写入 memory，读取时对当前 query 做 approximate kNN 搜索，再把检索到的 values 与局部 attention 输出做 gated fusion。memory 采用追加写入与 FIFO 丢弃，但每个新 document 开始会清空。 | 不属于 P3 | 同一长序列内累积的外部 key value memory，经 kNN 检索后与局部 attention 结果做 gated fusion。 | general | 检验一通过：检索回来的 value 直接进入隐藏状态计算，不是文本检索。检验二不通过：memory 主要服务单一 document 内长上下文建模，文档切换即清空，更接近序列级 long-context 机制而非跨 session task 或长程 agent episode 的经验复用。另一个 source-side 问题是其输入来自静态语料而非 agent 经验。 |
| [Li25g] | MAP-VLA: Memory-Augmented Prompting for Vision-Language-Action Model in Robotic Manipulation | 论文复用的是 human expert demonstrations，先把训练演示按任务阶段切分并对齐，形成 stage-level episodic task memory。方法不保存历史 KV，而是为每个阶段训练一组可学习 soft prompt 向量 \(V_k\) 来吸收该阶段经验；底座 VLA 冻结，只训练这些外部 prompt 参数。latent 先在离线阶段通过 prompt tuning 批量构建成 memory library，在线时根据当前轨迹与演示轨迹的相似度在局部阶段范围内检索合适 prompt。被选中的 stage-specific latent 以与 base prompt embedding 逐元素相加的形式组成新的输入 prompt，随后进入 VLA；库本身基本是静态写入，读取依赖 sliding-window trajectory matching 与 hierarchical stage constraint。 | 属于 P3 | 离线从演示阶段学习得到的 stage-specific soft prompt embeddings，在线检索后与 base prompt embedding 相加并作为模型输入前缀。 | embodied / robotics | 检验一通过：复用对象是 soft prompt 向量，直接进入后续 attention 与 hidden-state 计算，不是把演示文本重新塞回 prompt。检验二通过：这些 latent 由历史 demonstrations 离线构建并跨运行复用，目标是长程操作中的 episodic memory，而非单次推理提速。需要注意其经验源是专家演示而不是 agent 自生成日志。 |
| [Gup26] | ReasonCACHE: Teaching LLMs To Reason Without Weight Updates | 论文使用的是 human math demonstrations 与 teacher-style reasoning traces，主要来自 MetaMathQA 和过滤后的 OpenThoughts-3 一类长链条推理数据。方法通过 Prefix Tuning 在每一层直接学习软性的 prefix key value 对，而不是保存真实历史 forward-pass KV；主模型冻结，只训练这些 prefix 参数，或用一个小型辅助 MLP 生成它们。latent 在离线阶段训练好并在部署时冻结，可视为持久化的 reasoning skill cache。下游使用时，各层 prefix KV 与当前输入生成的 token KV 直接拼接成扩展后的 attention 字典，因此 attention 输出显式混合来自 prefix memory 与当前 token 的值；测试时没有在线写回或淘汰。 | 属于 P3 | 离线学得的分层 soft prefix K/V，对每层 attention 作为额外前缀键值直接拼接到 token-derived KV 前。 | reasoning | 检验一通过：prefix K/V 本体直接并入 attention 的 key value 集合，不是索引后再回填文本。检验二通过：该 latent 作为可移植的持久 reasoning cache 跨 query 复用，目标是固化推理技能而非单次上下文压缩。需说明其经验源以人类和教师生成 reasoning traces 为主，不是在线 agent rollout。 |
| [Wu25c] | TokMem: One-Token Procedural Memory for Large Language Models | 论文复用的是 procedure-response pairs，来源包括 Super-Natural Instructions 中的人类任务数据，以及 APIGen 一类整理或合成的 tool-use 数据；是否属于 agent 自执行经验，论文未把它定义得很强。方法把一个 procedure 压缩为单个可训练 memory token embedding，而不是保存真实 KV；主体 LLM 基本冻结，仅训练这些 memory token，本体前还有一次短暂 adaptation phase 用 LoRA 教模型理解 token 组合方式。latent 在离线记忆获取阶段训练出来，在线时由路由器根据 query 最终 hidden state 选择最相关 memory token。被召回的 token 直接附在 query 序列后参与自回归生成；写入是新增专属 token，更新时用 renormalization 校准不同 token 的范数，memory bank 持久存在。 | 边界 | 每个 procedure 对应一个持久 memory token embedding，经路由选中后拼接到输入序列并由模型注意力直接消费。 | general | 检验一通过：被复用的是连续 token embedding，本体直接进入模型计算，不是检索到文本 procedure 再塞回 prompt。检验二基本通过：memory token 面向跨任务跨会话 procedural reuse，而非同次推理加速。之所以标为边界，是因为 source side 更像静态 procedure dataset 与 tool-use instruction data 的压缩，agent 交互经验属性较弱。 |
| [Fu26] | LatentMem: Customizing Latent Memory for Multi-Agent Systems | 论文复用的是多智能体系统过往执行留下的 raw interaction trajectories，包含 agent index、输入提示与输出，来源是 agent 自生成历史 rollout，并在任务结束后不断写入 experience bank。方法先用外部 embedding 检索相关轨迹，再由一个轻量 Transformer memory composer 把这些 tokenized trajectories 与 agent role profile 压缩成固定长度 latent memory matrix；骨干 LLM 冻结，仅训练 composer，训练方式是 LoRA 加上 LMPO 强化学习目标。latent 不是离线一次性静态产物，而是在新 query 到来时在线检索后即时压缩出来。下游把得到的 latent memory 作为额外 latent tokens 与当前输入 hidden states 拼接，直接影响每个 agent 的 policy；bank 采用追加写入、相似度读取，论文未给出明确淘汰策略。 | 属于 P3 | 在线由 memory composer 把检索到的历史轨迹压缩成固定长度 latent token 矩阵，并与当前 hidden states 拼接后供各 agent 使用。 | multi-agent | 检验一通过：真正被消费的是 composer 生成的 latent tokens，它们直接进入 hidden-state 与 attention 计算；embedding 只负责检索候选轨迹。检验二通过：experience bank 跨任务跨运行持续积累并在后续 MAS 决策中复用，不是效率型缓存。其局限可能在于检索质量与 composer 压缩损失会共同影响 latent 的有效性。 |
| [Yu26d] | Dual Latent Memory for Visual Multi-agent System | 论文复用的是前序 agent 产生的 perceptual observations 与 cognitive thinking trajectories，来源是多智能体协作过程中 agent 自生成的感知和思考过程。方法为 perception 与 thinking 两条经验流分别构造连续 memory：前者从视觉编码器 hidden states 形成多粒度 perceptual latent，后者把连续 thought trajectory 经 entropy-driven chunking 与压缩模块整理为 thinking memory；主干 VLM 冻结，只训练各类 compressor、merge 或 refine 模块以及门控路由器。latent 在交互过程中持续在线建立与更新，推理时还会按高熵触发检索，对 top-k memory 做 refinement 后再插入当前解码位置。下游消费方式是把 refined memory 以一串 latent tokens 的形式直接注入 decoder 可注意的位置；系统还维护触发率驱动的保留、融合与淘汰机制，以控制容量。 | 属于 P3 | 感知与思考两类连续 latent memory tokens，按触发检索与精炼后插入 decoder 序列位置供语言模型直接注意。 | multi-agent | 检验一通过：被召回的是 refined latent memory，本体直接插入解码过程并由 decoder attention 消费。检验二通过：memory 服务于多 agent 长程协作中的历史经验复用，重点是跨 turn 的深度协作 episode，而非纯算力优化。需注明其持久性主要体现为同一协作过程中的长程记忆，论文对跨 session 库化复用写得较弱。 |
| [Shi25] | MemoryVLA: Perceptual-Cognitive Memory in Vision-Language-Action Models for Robotic Manipulation | 论文复用的是机器人操作中的历史视觉观察与任务语义线索，训练数据主要来自 human demonstrations，如 Open-X Embodiment、Bridge v2、RT-1 与 LIBERO。方法把当前时刻的多模态输入编码成两类连续表示：一类是由视觉 token 压缩得到的 perceptual latents，另一类是从语言模型 EOS hidden state 取出的 cognitive latent，并把它们写入 Perceptual-Cognitive Memory Bank；系统是端到端训练的，不是简单冻结 backbone 后外挂向量。latent 在运行时随每步观测在线产生并更新进 memory bank。读取时以当前 working memory 为 query 对 bank 做 cross-attention，取回的表示再通过 gated residual fusion 融入当前表征，最后送入 Diffusion Transformer action expert；bank 超容量时按相邻项 cosine similarity 做 merge 压缩。 | 属于 P3 | 在线维护的 perceptual 与 cognitive latent memory entries，经 cross-attention 读出后通过 gated residual 融合并条件化 DiT action generation。 | embodied / robotics | 检验一通过：bank 中的连续 memory 直接作为 cross-attention 的 key value，并经 gated fusion 进入动作生成。检验二通过：memory 明确保存 long-horizon manipulation 中的历史经验，用于后续决策，不是同次前向的省算缓存。需要注意它主要是同一 episode 内的历史复用，跨 session 持久库并非论文主轴。 |
| [Gao26] | Gated Memory Policy | 论文复用的是历史观察与动作轨迹，来源覆盖 human demonstrations、oracle teacher policy 生成数据，以及 heuristic policy 收集的数据；运行时也会把已发生的 history tokens 保留在窗口中。方法把图像经预训练 ViT 编码并池化成视觉 history token，把历史 action trajectory 经 MLP 编成 action token，再由独立的 cross-attention module 从这些缓存 token 中构造 memory 表征；整体 policy 是训练态，记忆门控先单独校准后冻结，最终再训练 unified policy。latent 在推理时随时间步在线写入固定窗口缓存，不需要把历史文本化。下游通过 gated cross-attention 把 history memory 输出与当前状态残差相加，是否启用由二值 memory gate 决定；窗口写入是滑动追加，读取是按 gate 触发的按需 cross-attention，淘汰由固定窗口隐式完成。 | 属于 P3 | 由历史视觉与动作 token 编成的连续 history tokens，作为 cross-attention 的 key value 被按需读取，并经 gate 控制残差注入当前 policy。 | embodied / robotics | 检验一通过：history tokens 本体直接进入 cross-attention 与后续 hidden-state 计算，不是只作为检索键。检验二通过：方法显式承担跨 trial 与长程任务中的经验复用，尤其用于推断摩擦或重量等需多次交互累积的信息，而非单次推理加速。其特点是存在 selective gate，使 latent 何时被读成为学习目标的一部分。 |
| [Koo25] | HAMLET: Switch your Vision-Language-Action Model into a History-Aware Policy | 论文复用的是机器人任务中的历史视觉观察与动作相关上下文，训练来源主要是 human demonstrations，覆盖 RoboCasa、LIBERO、BridgeV2 等数据以及真实机器人示教。方法引入可学习的 moment tokens，把它们与图像和指令一起送入 VLM，得到每步的 latent summary \(m'_t\)；该 summary 再被写入一个浅层 Transformer memory module，作为后续时刻的历史表示。不同 backbone 设置下训练策略不同：在 GR00T N1.5 上 VLM 与 moment tokens 冻结，只训练 memory module 与 action head；在 CogACT 设置下 VLM 与 moment tokens 可继续训练。latent 在每个时间步在线生成并进入固定长度历史缓冲区，下游先经 memory module 对窗口内历史做 causal self-attention，再把增强后的历史特征与当前 VLM 表征拼接，送入 DiT 或 flow-matching action expert。 | 属于 P3 | 每步由 VLM 生成并缓存的 moment-token latent summaries，经浅层 memory Transformer 读出后与当前表征拼接并条件化 action expert。 | embodied / robotics | 检验一通过：moment-token latent 直接进入后续 hidden-state 与 attention 计算，不是文本检索。检验二通过：它承担长程操作 episode 中的历史经验复用，论文目的在 history-aware control 而非 cache efficiency。需要注明其主记忆是同一 session 内窗口化缓存，跨 session 持久写回不是重点。 |
| [Guo26] | Chameleon: Episodic Memory for Long-Horizon Robotic Manipulation | 论文处理的是长程机器人操作中的多模态历史观察，训练来源是 teleoperated human demonstrations；运行时每步观测还会继续驱动内部记忆状态更新。方法先把输入转成 patch tokens，再通过 spatial temporal anchoring 与 Mamba-style selective SSM 把这些 token 持续压缩为 spatiotemporal slot features 和 episodic hidden states，而不是保存显式 KV cache；视觉编码器冻结，memory stack、imagination head 与 flow-matching policy 等部分训练。latent 在执行过程中在线产生并按状态空间动力学连续更新，没有离线预建 memory library。下游把最终 latent state 投影成 conditioning token，作为 policy Transformer 每一层 cross-attention 的条件输入；信息保留与遗忘由 learned timescale 和 selective update 控制，没有显式检索式淘汰。 | 属于 P3 | 由 patch-token 经过 anchoring 与 selective SSM 演化得到的 episodic latent states，投影成 conditioning token 后通过 cross-attention 条件化策略网络。 | embodied / robotics | 检验一通过：最终 latent conditioning token 直接进入 policy attention，不是索引后回填离散经验。检验二通过：memory 用于同一长程 manipulation episode 的历史经验保存与复用，目标是处理 non-Markov 决策，不是效率优化。需补充的是，它更像在线 recurrent latent memory，而不是跨 session 检索库。 |
| [Lin25d] | EchoVLA: Synergistic Declarative Memory for VLA-Driven Mobile Manipulation | 论文复用的是移动操作中的 expert trajectories，来源同时包含自动化管线生成的 teacher-style 轨迹与真实机器人 teleoperation 数据。方法先把语言、RGB、3D 结构和 proprioception 编成统一 token 序列，再维护两类连续 memory：scene memory 把 3D 特征聚合为 voxelized feature volume，episodic memory 则保留时间索引的历史编码 token；SigLIP 编码器冻结，3D encoder、memory interaction 层和 diffusion denoiser 等部分训练。latent 在运行时在线写入，scene memory 还会根据 reconstruction discrepancy 做局部更新，episodic memory 按固定窗口 FIFO 保持最近历史。下游使用当前观测去匹配相关 memory，再经 coarse fine-grained cross-attention 得到 memory-augmented representation，最后作为 base 与 arm 两个 diffusion action denoiser 的条件输入。 | 属于 P3 | voxelized 3D scene latent 与时间索引的 episodic token features，经检索和 cross-attention 交互后组成 memory-augmented representation 来条件化 diffusion policy。 | embodied / robotics | 检验一通过：被读出的 scene 与 episodic memory 都以连续特征形式直接进入 cross-attention 和动作生成。检验二通过：scene memory 明确跨 episode 持续积累，同时时序 episodic memory 支持单次长任务中的历史复用，因此不只是临时缓存。它是典型 hybrid memory：场景记忆偏环境级持久表征，episodic 记忆偏近程经历。 |
| [Zhu26] | Hybrid Self-evolving Structured Memory for GUI Agents | 论文复用的是 GUI 成功轨迹，来源同时包括 GUIAct、Mind2Web 的 human demonstrations，以及训练中 agent 自生成的高置信 rollout。方法把每条 trajectory 一方面建成图中的 trajectory node 与结构边，另一方面用 Q-Former 风格的 VLM memory encoder 把整条轨迹压缩成 8 个 continuous embeddings；encoder 通过 PEFT 训练，LoRA 只覆盖少量参数。latent 随新轨迹到来逐步生成并写入自演化全局记忆，推理时先做 CLIP-based seed retrieval 与图上 multi-hop expansion，再把命中的连续 embeddings 放入 working memory。下游消费方式是把这 8 个 embeddings 直接拼接到 GUI agent 的 embedding layers；系统还用 judge 驱动 ADD、MERGE、REPLACE 以及冗余控制，维持图结构与 latent memory 的共同演化。 | 边界 | 每条 GUI trajectory 压缩得到的 8 个 continuous embeddings，经结构化检索选中后直接拼接进 GUI agent embedding layers；另有独立 CLIP 向量用于索引。 | GUI / mobile | 检验一通过：被实际复用的 continuous embeddings 直接进入 embedding layer 与后续 attention 计算，不是单纯索引。检验二通过：全局 memory 跨会话跨网站长期积累成功 GUI 经验，明显具有经验复用语义。之所以标为边界，是因为系统本质上是 hybrid 载体：图结构节点、边与 CLIP 索引承担了很大一部分记忆组织和检索工作，连续 latent 只是其中一条并行通道。 |