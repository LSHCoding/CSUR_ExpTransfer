## A. Completed table

| Work | Acquisition Mechanism | Latent Encoding | Memory Structure | Retrieval | Fusion | Modality |
|------|------------------------|-----------------|------------------|-----------|--------|----------|
| [Che25b] | Cache-based | KV/hidden states | single-stream | similarity | attention-injection | text |
| [Sun26] | Cache-based | prefix K/V | multi-stream | similarity | additive | embodied |
| [Wu22] | out-of-scope | out-of-scope | out-of-scope | out-of-scope | out-of-scope | out-of-scope |
| [Li25g] | Prompt-based | soft prompt | single-stream | similarity | additive | embodied |
| [Gup26] | Cache-based | prefix K/V | single-stream | always-injected | attention-injection | text |
| [Wu25c] | Module-based | memory token | single-stream | query-routed | concatenation | text |
| [Fu26] | Module-based | memory token? | multi-stream | similarity | concatenation | text |
| [Yu26d] | Module-based | KV/hidden states | multi-stream | similarity | attention-injection | cross-modal |
| [Shi25] | Module-based | memory token | multi-stream | similarity | gated-residual | embodied |
| [Gao26] | Cache-based | memory token? | multi-stream | gated-admit | gated-residual | embodied |
| [Koo25] | Module-based | memory token | multi-stream | always-injected | concatenation | embodied |
| [Guo26] | Module-based | SSM latent | multi-stream | query-routed? | SSM-injection | embodied |
| [Lin25d] | Module-based | out-of-vocab: feature volume + episodic tokens | multi-stream | similarity | cross-attention | embodied |
| [Zhu26] | Module-based | out-of-vocab: discrete+continuous graph nodes | out-of-vocab: graph-structured memory | out-of-vocab: kNN + graph expansion | out-of-vocab: discrete prompt + continuous concat | GUI |

## B. Evidence and notes

### [Che25b] — read: full-text

- Acquisition Mechanism: §2.2 — “using KV values as the log representation” → Cache-based.
- Latent Encoding: §2.2 — “represent logs using KV values” → KV/hidden states.
- Memory Structure: §2.2 — “concatenate them and use them” → single-stream.
- Retrieval: §2.1 — “highest cosine similarity” → similarity.
- Fusion: App. D — “concatenated KV values… augment LLM generation” → attention-injection.
- Modality: §3.1 — reasoning / knowledge tasks only → text.
- Note: fusion is implemented by KV concatenation; labeled attention-injection because the reused latent enters as attention cache.

### [Sun26] — read: full-text

- Acquisition Mechanism: §III-C, Fig. 2 — “maintain a FIFO buffer” → Cache-based.
- Latent Encoding: §III-C — “layer-wise FIFO prefix K/V” → prefix K/V.
- Memory Structure: §III-C — separate memory-enabled layers maintain their own caches → multi-stream.
- Retrieval: §III-D, Eq. 4 — “address-space matching” → similarity.
- Fusion: §III-F, Eq. 6 — “residual loading” of retrieved K/V → additive.
- Modality: §I — VLA robotic manipulation → embodied.

### [Wu22] — read: full-text

- Acquisition Mechanism: §3.1 — “cache of the prior M (key, value) pairs”; row marked out-of-scope.
- Latent Encoding: §1 / §3.1 — generic LM hidden-state memory; row marked out-of-scope.
- Memory Structure: §3 — sequential long-document LM only; row marked out-of-scope.
- Retrieval: §1 — “approximate kNN lookup”; row marked out-of-scope.
- Fusion: §3.1, Eq. 1–2 — learned gate over local and memory attention; row marked out-of-scope.
- Modality: §1 — webtext / books / code → text, but row marked out-of-scope.
- Note: out-of-scope for this survey because the paper is generic language modeling, not agent experience with decision-process semantics.

### [Li25g] — read: full-text

- Acquisition Mechanism: §III-B — “learnable soft tokens… optimized via prompt tuning” → Prompt-based.
- Latent Encoding: §III-B — “vector sequence… as soft prompt” → soft prompt.
- Memory Structure: §III-B, Fig. 2 — one retrieved stage prompt enters the model → single-stream.
- Retrieval: §III-C, Eq. 4 — trajectory-window matching by distance → similarity.
- Fusion: §III-B, Eq. 2 — base prompt plus memory prompt by element-wise addition → additive.
- Modality: §I — long-horizon robotic manipulation → embodied.
- Note: the paper also ensembles actions in Eq. 7; I used additive because this column asks how the selected latent enters the forward pass.

### [Gup26] — read: full-text

- Acquisition Mechanism: §2, Fig. 1 — “compresses demonstrations into a compact cache” → Cache-based.
- Latent Encoding: §2.2 — “prefix key–value vectors at each attention layer” → prefix K/V.
- Memory Structure: §2.2 — one fixed prefix cache per layer, shared at inference → single-stream.
- Retrieval: §2.4 — “prefix is computed once and cached” → always-injected.
- Fusion: §2.2 — augmented keys and values are prepended to attention → attention-injection.
- Modality: §3.1 — GSM8K / MATH / GPQA / AIME → text.
- Note: implementation uses matrix concatenation of prefix K/V, but the functional entry point is attention injection.

### [Wu25c] — read: full-text

- Acquisition Mechanism: §1 — “modular framework” with dedicated memory embeddings → Module-based.
- Latent Encoding: §2.2, Eq. 1 — “single trainable memory token” → memory token.
- Memory Structure: Fig. 1 — selected token is appended into one token stream → single-stream.
- Retrieval: §2.3, Eq. 4 — “memory routing” from query hidden state → query-routed.
- Fusion: §2.3 — “append it to form [q; a_m*]” → concatenation.
- Modality: §3 — QA-style text tasks / tool-style procedures → text.

### [Fu26] — read: full-text

- Acquisition Mechanism: §4.1 — “learnable memory composer” → Module-based.
- Latent Encoding: §4.1 — “appended… as additional latent tokens” → memory token?.
- Memory Structure: §2 / Fig. 2 — agent-specific memories for heterogeneous roles → multi-stream.
- Retrieval: §4.2, Eq. 3 — cosine-similarity retrieval over experience bank → similarity.
- Fusion: §4.3 — “concatenated to h_j” → concatenation.
- Modality: §5.1 — coding / QA / planning benchmarks → text.
- Note: `memory token?` because the paper could also be read as `composed latent`; I chose the concrete forward-pass form.

### [Yu26d] — read: full-text

- Acquisition Mechanism: §3.2 — “learnable compression module” → Module-based.
- Latent Encoding: §3.2 — memory units persisted as key-value pairs → KV/hidden states.
- Memory Structure: §3.2 — separate perception and thinking memories → multi-stream.
- Retrieval: §3.3 — “similarity-based top-k memory units” → similarity.
- Fusion: §3.3 — latent memories are “inserted at the triggering position” → attention-injection.
- Modality: Abstract / §1 — visual inputs plus agent communication / reasoning → cross-modal.

### [Shi25] — read: full-text

- Acquisition Mechanism: §3.1, Fig. 2 — dedicated Perceptual-Cognitive Memory Bank → Module-based.
- Latent Encoding: §3.2–§3.3 — compact perceptual and cognitive tokens → memory token.
- Memory Structure: §3.3 — two maintained streams in PCMB → multi-stream.
- Retrieval: §3.3, Eq. 6 — scaled dot-product access to memory → similarity.
- Fusion: §3.3, Eq. 8 — gated mixture of retrieved and current states → gated-residual.
- Modality: §3.1 — robotic manipulation with visual observations and actions → embodied.

### [Gao26] — read: full-text

- Acquisition Mechanism: §III-B — “cache a sliding window of n history trajectories” → Cache-based.
- Latent Encoding: §III-B, Fig. 2 — cached image/action token embeddings → memory token?.
- Memory Structure: Fig. 2(b) — separate history-conditioning path → multi-stream.
- Retrieval: Eq. 2, Fig. 2(b) — binary memory gate decides recall → gated-admit.
- Fusion: §III-B — \(\bar z = \mu h + z\) → gated-residual.
- Modality: §II-A / §III-B — image observations plus robot actions → embodied.
- Note: `memory token?` because the cache stores token embeddings, not purpose-trained memory tokens.

### [Koo25] — read: full-text

- Acquisition Mechanism: §3 / §3.1 — lightweight memory module over moment tokens → Module-based.
- Latent Encoding: §3.1, Eq. 3 — “moment tokens” → memory token.
- Memory Structure: Fig. 2 / §3.2 — separate memory-module path from main VLA path → multi-stream.
- Retrieval: §3.2, Eq. 5–6 — most recent \(T\) moment tokens are always processed → always-injected.
- Fusion: §3.2, Eq. 7 — concatenate history-augmented feature with VLM state → concatenation.
- Modality: §3 — VLA policy for robot control → embodied.

### [Guo26] — read: full-text

- Acquisition Mechanism: §3.3 — differentiable episodic/working memory architecture → Module-based.
- Latent Encoding: §3.3, Eq. 11–13 — episodic and working states updated by SSMs → SSM latent.
- Memory Structure: Abstract / §3.3 — “couples an episodic state with a working state” → multi-stream.
- Retrieval: §3.3, Eq. 14 — task query selects relevant working summaries → query-routed?.
- Fusion: §3.3, Eq. 13 — retrieved state injected through SSM update → SSM-injection.
- Modality: §1 / §5 — long-horizon robotic manipulation → embodied.
- Note: `query-routed?` because episodic-to-working transfer is continuous, but final task-conditioned readout is query-based.

### [Lin25d] — read: full-text

- Acquisition Mechanism: §3.3 — dual memory banks introduced as dedicated modules → Module-based.
- Latent Encoding: §3.3, Eq. 3–4 — 3D scene feature volume plus episodic token FIFO → out-of-vocab.
- Memory Structure: Fig. 1(c) / §3.3 — scene memory and episodic memory → multi-stream.
- Retrieval: §3.3 — “cosine similarity” matching to memory entries → similarity.
- Fusion: §3.3, Eq. 5–6 — selected entries are read by cross-attention → cross-attention.
- Modality: §1 / §3.4 — mobile manipulation with base and arm control → embodied.
- Note: the two retrieved streams are concatenated later in Eq. 7; I used cross-attention because that is how memory enters the backbone.

### [Zhu26] — read: full-text

- Acquisition Mechanism: §3.2 — retrieval, redundancy checking, structured update pipeline → Module-based.
- Latent Encoding: §3.1 — node tuple contains symbolic fields plus trajectory embedding → out-of-vocab.
- Memory Structure: §3.1 — evolving graph \(G=(V,E)\) → out-of-vocab.
- Retrieval: §3.3 — kNN seed retrieval then graph expand-and-rerank → out-of-vocab.
- Fusion: §3.3 — discrete guidance instructions plus continuous trajectory evidence → out-of-vocab.
- Modality: §1 / §4 — GUI-agent benchmarks → GUI.
- Note: in-scope overall, but it does not fit a single-channel latent-memory vocabulary without distortion.

## Next steps

- I can write this into a project file as a cleaned extraction sheet.
- I can also do a normalization pass on the borderline rows `[Fu26]`, `[Gao26]`, `[Guo26]`, `[Lin25d]`, `[Zhu26]` to make the table internally stricter.