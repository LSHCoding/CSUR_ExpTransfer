
| 引用标识 | 标题 | 关键思想与方法 | P7 归属判定 | 外化能力类型 | 环境合成度 | 产物形态 | 领域 | 备注 |
|---|---|---|---|---|---|---|---|---|
| [Pah25] | Explorer: Scaling Exploration-driven Web Trajectory Synthesis for Multimodal Web Agents | 该文以 GPT-4o 为 teacher backbone，分化出 task proposer、refiner、summarizer 与 verifier 等角色，输入端是网站首页的 screenshot、HTML 与 accessibility tree，而不是已有 trajectory。[Pah25] 通过在真实网站中逐步探索、边执行边细化任务描述，生成与当前页面状态对齐的 grounded task 和完整 multimodal web trajectory。[Pah25] 保存的 artifact 是带 screenshot、SoM 图、HTML、A11y tree、自然语言动作与思考的成功轨迹，并用 LLM verifier 做 trajectory-level 通过/淘汰过滤，额外清除异常滚动等噪声。[Pah25] 最终 Explorer dataset 被物化为独立数据资源，用于 student multimodal web agent 的 SFT。 | 属于 P7 | Policy(行为外化) | 真实环境锚定 | 真实网站交互轨迹数据，含 screenshot-thought-action 与页面结构信息，用于 web agent SFT。 | web | 检验一通过：主体内容来自 teacher 在真实网站中的探索生成，不是对输入轨迹做重写。[Pah25] 检验二通过：产物被整理为 Explorer 数据集，供外部 student 消费。[Pah25] 检验三通过：主要产物是 trajectory carrier，不是环境本身。[Pah25] 目标载体以 Narrative Tokenized 为主，含较强 multimodal 顺序结构。 |
| [Tra25] | Towards Internet-Scale Training For Agents | 该文的 InSTA 流程把多个 LLM 分成 task proposer、data-collection agent 与 judge，输入端是从 Common Crawl 派生的大规模真实网站集合及其在线页面内容。[Tra25] proposer 先为 URL 合成任务，agent 再在真实网页上经 Playwright 执行并产生 reasoning-action trace，judge 对整条轨迹给出 success 判断与 reasoning trace。[Tra25] 论文保存了大规模 screenshot、action reasoning、trajectory 与 judge trace，并用 task achievability 检查、judge 校准和 success 过滤构成质量控制。[Tra25] 这些 artifact 被打包为独立数据集供较小 student 做 SFT，同时论文也把“互联网规模环境解锁”作为核心贡献之一。 | 边界 | 兼有 | 真实环境锚定 | 真实网页轨迹与 judge trace 数据，被用于 web agent SFT。 | web | 检验一通过：数据主体来自模型在真实网站上的任务生成与执行，而非输入现成轨迹。[Tra25] 检验二通过：insta-150k-v2 被物化为独立数据资源，供 student 使用。[Tra25] 检验三边界：论文既产出 carrier dataset，也把 internet-scale environment 与其管线当作核心结果。[Tra25] Policy 产物是主，Evaluator trace 为辅；若正文只收更“纯”的 carrier 外化，可放边界位。 |
| [Tao26] | RAGShaper: Eliciting Sophisticated Agentic RAG Skills via Automated Data Synthesis | 该文以 gpt-oss-120b 为 teacher，输入端是由 Wikipedia seed entity 出发构建的知识树与 distractor-curated knowledge base，而非已有 agent log。[Tao26] 流程先做信息整理与问题答案反向合成，再让 teacher agent 在合成的 distractive KB 中执行受约束的 retrieval-navigation，产出能暴露复杂 RAG 技能的 reasoning trajectory。[Tao26] 最终 artifact 是 \(Q,A,\mathcal{T}\) triplet，含问题、标准答案与完整 agent trajectory，并用答案匹配与 F1 阈值筛掉低质量样本。[Tao26] 这些 triplet 被物化为训练语料，用于 student RAG agent 的 SFT。 | 属于 P7 | Policy(行为外化) | 构造式合成 | 从合成知识库中诱导出的 RAG reasoning trajectory triplet，用于 agentic RAG SFT。 | RAG | 检验一通过：输出不是对现成 trajectory 改写，而是从 evidence tree 与 teacher 能力反向诱导新轨迹。[Tao26] 检验二通过：轨迹被整理为独立训练集供 student 使用。[Tao26] 检验三通过：主要产物是 trajectory carrier，不是 KB 生成装置本身。[Tao26] 虽含合成知识环境，但主结果仍是 Narrative Tokenized 轨迹。 |
| [Lu26] | Structured Distillation of Web Agent Capabilities Enables Generalization | 该文把 Gemini 3 Pro 拆成 persona generator、task generator、agent annotator 与 judge，输入端是 WebArena 环境访问、persona 采样与标注指令，而不是已有人类 demonstration。[Lu26] task generator 先在环境中探索并生成 intent 与 evaluation hints，agent 再仅根据 intent 执行得到 step-by-step web trajectory 与 reasoning trace。[Lu26] 产物包括任务 intent、evaluation hints、trajectory、thought trace 与 success label，质量控制主要依赖 judge 的反向验证协议，只保留成功轨迹进入 A3-SYNTH。[Lu26] 这些数据被作为独立监督资源，用于 student web agent 的多轮 SFT。 | 属于 P7 | 兼有 | 真实环境锚定 | 真实网页 trajectory 数据，附带 evaluation hints 与 success annotation，用于 web agent SFT。 | web | 检验一通过：主体由 teacher 在环境中的任务生成与执行产生，不是对输入轨迹做抽象。[Lu26] 检验二通过：A3-SYNTH 是独立可复用数据集。[Lu26] 检验三通过：主要产物是任务与轨迹等 carrier。[Lu26] Policy 产物是主；evaluation hints 与 success annotation 构成较弱 Evaluator 外化。目标载体主要是 Narrative Tokenized，evaluation hints 带轻度 Schematic 成分。 |
| [Xu25o] | TOUCAN: Synthesizing 1.5M Tool-Agentic Data from Real-World MCP Environments | 该文以多种 open model 充当 task generator、trajectory generator 与 quality annotator，输入端是近 500 个真实 MCP server 的 tool specification，而不是已有 tool trajectory。[Xu25o] 流程先做 server onboarding，再做 task synthesis、task filtering、trajectory generation 与 post-filtering，agent 直接在真实 MCP 环境里执行工具并收集 reasoning、tool call 与 response。[Xu25o] 最终保存的是多轮消息轨迹、target tools、task quality assessment、response quality assessment 与元数据；验证同时包含规则检查与 LLM 质量评估。[Xu25o] 产物被发布为 1.5M tool-agentic dataset，主要用于 open-source model 的 SFT。 | 属于 P7 | Policy(行为外化) | 真实环境锚定 | 真实 MCP 执行轨迹与对话数据，用于 tool-agent SFT。 | tool-use / function-calling | 检验一通过：主体来自模型基于 tool spec 的任务与轨迹生成，不是对输入轨迹重表述。[Xu25o] 检验二通过：Toucan-1.5M 是独立数据资源。[Xu25o] 检验三通过：主要产物是 tool-use carrier，而非 MCP 环境本体。[Xu25o] 保留了 model-based quality assessment，但论文主用法仍是质量控制，Evaluator 外化不是主轴。产物既有 Narrative 对话，也含明显 Schematic tool-call 结构。 |
| [Li25at] | From Evidence to Trajectory: Abductive Reasoning Path Synthesis for Training Retrieval-Augmented Generation Agents | 该文用 LLaMA 3.1-70B 作为 teacher，输入端是问题、标准答案与 golden evidence，而不是已有 agent trajectory。[Li25at] 方法通过 abductive reasoning 先反推 latent reasoning graph，再把 sub-question answering 与 planner-executor 对话组织成可训练的 agent trajectory，生成阶段基本不依赖真实交互环境。[Li25at] 保存的 artifact 是 process-supervised RAG dialogue trajectory，含 planner prompt、executor prompt 与中间 reasoning path，并借助 evidence matching 与最小充分性约束做质量控制。[Li25at] 最终 265k reasoning path 被物化为训练集，用于 student RAG agent 的 SFT。 | 属于 P7 | Policy(行为外化) | 构造式合成 | 从 question-answer-evidence 反向构造的 RAG 对话轨迹，用于 planner-executor 式 agent SFT。 | RAG | 检验一通过：输入不是现成轨迹，外化内容来自 teacher 对 evidence 与 answer 的反向构造能力。[Li25at] 检验二通过：生成结果是独立数据资源而非一次性推理。[Li25at] 检验三通过：主要产物是 reasoning trajectory carrier。[Li25at] 这是很典型的构造式合成 P7；目标载体是 Narrative Tokenized。 |
| [Wan25as] | LLMs as Scalable, General-Purpose Simulators For Evolving Digital Agent Training | 该文把 GPT-4o-mini 分别用于 world simulator 与 teacher agent，输入端是状态摘要、动作历史与任务选择信号。[Wan25as] simulator 负责预测 UI state transition，teacher 在该模拟环境中探索并生成动作、instruction 与 step-wise reasoning，最终形成训练轨迹。[Wan25as] 论文同时保存 trajectory、instruction、thought 与结构化 UI state，并用 trajectory wrapper 和难度选择策略过滤低质量或失真样本。[Wan25as] 这些产物被用于 student 数字代理的微调，但论文的中心贡献同样包含可扩展 world simulator 本身。 | 边界 | Policy(行为外化) | 模拟环境 | LLM 模拟 UI 环境中生成的 trajectory 与 instruction 数据，用于 digital agent 微调。 | GUI / mobile | 检验一通过：数据主体来自参数化 simulator 与 teacher 的联合生成，而非输入轨迹改写。[Wan25as] 检验二通过：轨迹被物化为可训练数据并供 student 使用。[Wan25as] 检验三边界：论文同时把 simulator 作为主产物，environment 生成占比很高。[Wan25as] 主载体是 Narrative trajectory，但环境动态外化是其不可忽略的一半。 |
| [Wan25ar] | SynthAgent: Adapting Web Agents with Synthetic Supervision | 该文以 GPT-4.1 为 teacher，输入端是新网站环境中的 screenshot、A11y tree 与按功能分组的元素集合，而不是现成 demonstration。[Wan25ar] teacher 先做 categorized exploration 生成高层任务，再在执行过程中对任务进行 conflict-triggered refinement，随后对采集到的轨迹做 post-hoc trajectory refinement，清除冗余和错位步骤。[Wan25ar] 最终 artifact 是 refined task 与 refined noise-free trajectory 的配对数据集，并通过执行期冲突检测与离线轨迹清洗做质量控制。[Wan25ar] 这些数据被用来对 student web agent 做环境适配 SFT。 | 属于 P7 | Policy(行为外化) | 真实环境锚定 | 新网站上的 refined task-trajectory 对，用于 web agent SFT。 | web | 检验一通过：虽然有 exploration 过程，但最终监督主体来自 teacher 在目标环境中生成并修订的新轨迹，不是对输入轨迹直接重写。[Wan25ar] 检验二通过：\(D=\{(\tau^*,h^*)\}\) 被物化为独立训练集。[Wan25ar] 检验三通过：主要产物是 trajectory carrier。[Wan25ar] 目标载体是 Narrative Tokenized。 |
| [Awa25] | Fara-7B: An Efficient Agentic Model for Computer Use | 该文中的 FaraGen 使用 LLM 完成 task proposal、multi-agent task solving 与 verifier judging，输入端是 seed URL、网页索引与 exemplar task，而不是已有交互轨迹。[Awa25] teacher 通过真实网页上的 headless browser 执行生成 screenshot-thought-action 序列，并由 orchestrator ledger 与多种 verifier 监控循环、关键点与任务满足情况。[Awa25] 最终保存的是计算机使用轨迹，含像素截图、推理文本与原子动作；verifier 主要负责过滤成功样本，没有明确把 rubric 输出作为独立监督保留。[Awa25] 这些轨迹被用来训练 Fara-7B，并构成可复用的数据资产，尽管全文对训练集是否完整公开写得不够明确。 | 属于 P7 | Policy(行为外化) | 真实环境锚定 | 真实网页 computer-use screenshot-thought-action 轨迹，用于 CUA 模型 SFT。 | GUI / mobile | 检验一通过：主体来自 teacher 在真实网页中的执行生成。[Awa25] 检验二通过：样本先被物化为独立训练数据供 student Fara-7B 消费；公开发布范围未完全写清，但“独立数据资源”这一点成立。[Awa25] 检验三通过：主要产物是 trajectory carrier。[Awa25] Evaluator 信号更像内部过滤，不单列为评估外化。 |
| [Yan25ab] | ToolMind Technical Report: A Large-Scale, Reasoning-Enhanced Tool-Use Dataset | 该文用 LLM 扮演 user agent、assistant agent 与 tool agent，输入端是从开源数据收集的函数集、函数图与采样出的 function chain，而不是已有多轮 trajectory。[Yan25ab] 多智能体在模拟环境中生成 multi-turn tool-use dialogue，assistant turn 内同时保留 think、content 与 tool call，形成 reasoning-enhanced 数据。[Yan25ab] 质量控制包括高层 goal alignment 与 turn-level procedural correctness 两级过滤，错误步骤主要被删除或屏蔽，验证标签本身未作为主要独立监督字段强调保存。[Yan25ab] 最终 36 万级样本被作为独立数据集，用于 tool-use 模型的 SFT。 | 属于 P7 | Policy(行为外化) | 模拟环境 | 模拟 user-assistant-tool 多轮对话与 tool-call 数据，用于 tool-use model SFT。 | tool-use / function-calling | 检验一通过：主体来自参数化多代理模拟，不是对输入轨迹重写。[Yan25ab] 检验二通过：ToolMind 是独立可复用数据集。[Yan25ab] 检验三通过：主要产物是对话与 tool-call carrier。[Yan25ab] turn-level validation 更接近清洗机制；目标载体兼具 Narrative 对话与 Schematic tool-call 结构。 |
| [Che26d] | OpenMobile: Building Open Mobile Agents with Task and Trajectory Synthesis | 该文以 Gemini-3.1-Pro-Preview 作为 expert 与 monitor，并利用 early-stage learner checkpoint 共同生成数据；输入端包含由随机探索得到的 exploration trajectory，用于建立 Global Environment Memory。[Che26d] 在此基础上，模型先从全局功能记忆合成多步任务，再通过 error-intervention switching 在 AndroidWorld 模拟环境中生成轨迹，并由 expert 重写 step-level CoT。[Che26d] 产物是任务指令、轨迹动作与每步 reasoning，质量控制覆盖 instruction 打分、去重、数量上限与执行偏差监控。[Che26d] 这些数据被作为开放训练集，用于 mobile agent 的 SFT 与 RL。 | 边界 | Policy(行为外化) | 模拟环境 | AndroidWorld 中的 mobile task instruction 与 action-reasoning trajectory，用于 mobile agent SFT 与 RL。 | GUI / mobile | 检验一边界：最终监督不是对某一条输入轨迹直接改写，但任务合成明显依赖先前 exploration trajectory 建立的环境记忆。[Che26d] 检验二通过：任务与轨迹被物化为独立训练数据。[Che26d] 检验三通过：主要产物是 carrier，不是环境本体。[Che26d] 若正文对“输入端必须极干净地来自参数能力”要求更严，可放边界位。 |
| [Kan26b] | Learning with Challenges: Adaptive Difficulty-Aware Data Generation for Mobile GUI Agent Training | 该文用 explorer、supervisor 与 synthesizer 等 VLM 角色围绕 student 能力轮廓生成数据，输入端是 GUI observation、SoM、UI tree 与已有能力 profile，而不是待转写的 trajectory。[Kan26b] 它先依据 prior dataset 对 student 做 profiling，再按 challenge point 采样目标难度，在 emulator 中生成 goal-conditioned mobile trajectory，最后用 inverse synthesis 重建 thought 与 instruction。[Kan26b] 产物包括 screenshot、UI tree、低层动作、step-level reasoning 与 trajectory-level instruction，并通过 difficulty-aware curriculum 和 VLM judge 过滤高分样本。[Kan26b] 这些轨迹被整理为独立训练数据，用于 mobile GUI agent 的 SFT。 | 属于 P7 | Policy(行为外化) | 模拟环境 | 难度可控的 mobile screenshot-thought-action 轨迹与指令，用于 GUI agent SFT。 | GUI / mobile | 检验一通过：prior dataset 只用于能力画像与采样控制，最终外化主体仍是新生成轨迹。[Kan26b] 检验二通过：生成结果被作为独立训练数据使用。[Kan26b] 检验三通过：主要产物是 trajectory carrier。[Kan26b] 带明显 curriculum 设计，但不改变其 P7 性质。 |
| [Pra25b] | APIGen-MT: Agentic Pipeline for Multi-Turn Data Generation via Simulated Agent-Human Interplay | 该文用 LLM 充当 blueprint generator、review committee、simulated human 与 test agent，输入端是 API dependency graph、domain rules、reference data、persona 与少量 few-shot 示例。[Pra25b] 流程先生成并验证 blueprint，再在可执行但模拟的人机交互环境中，让 agent 与 simulated human 多轮对话并调用 API，直到达成 blueprint 指定目标。[Pra25b] 保存的 artifact 是多轮 dialogue、tool call、环境反馈与目标状态对应的完整 interaction trajectory，并通过 execution checker、committee semantic review 与 rejection sampling 控制质量。[Pra25b] APIGen-MT-5k 被作为独立数据集发布，用于行为克隆训练 function-calling agent。 | 属于 P7 | Policy(行为外化) | 模拟环境 | 模拟 agent-human 多轮 API 交互轨迹，用于 behavior cloning。 | tool-use / function-calling | 检验一通过：主体来自模型基于 blueprint 的新生成交互，不是对现成 trajectory 做改写。[Pra25b] 检验二通过：APIGen-MT-5k 是独立数据集。[Pra25b] 检验三通过：主要产物是 multi-turn carrier，不是环境本身。[Pra25b] 目标载体以 Narrative 对话为主，内嵌 Schematic function-call。 |
| [Tan23] | ToolAlpaca: Generalized Tool Learning for Language Models with 3000 Simulated Cases | 该文以 ChatGPT 与 GPT-3.5 分别扮演 documentation generator、user agent、assistant agent 与 tool executor，输入端是从 public API 资源收集的工具名与简短介绍，而不是已有 tool-use log。[Tan23] 它先把粗糙工具描述扩写成结构化文档，再通过 user-assistant-tool 三方模拟生成多轮工具使用实例，tool response 由 LLM 依据 OpenAPI 规格模拟。[Tan23] 产物包括标准化 tool documentation 与 \{Instruction, Actions, Response\} 三元组数据，并用自动规则和少量人工抽检筛除不可解析或低质量样本。[Tan23] 最终 ToolAlpaca corpus 被作为独立资源，用于小模型的工具学习微调。 | 属于 P7 | Policy(行为外化) | 模拟环境 | 模拟 tool-use 指令-动作-响应三元组与工具文档，用于 tool-learning SFT。 | tool-use / function-calling | 检验一通过：数据主体来自模型对工具文档的扩写与模拟生成，不是对输入轨迹重表述。[Tan23] 检验二通过：语料被物化为独立 corpus 供 student 使用。[Tan23] 检验三通过：主要产物是 carrier。[Tan23] 代理性较弱、偏早期 tool-calling，但就 P7 三检验而言是正例。目标载体含明显 Schematic tool documentation。 |
| [Liu24n] | APIGen: Automated Pipeline for Generating Verifiable and Diverse Function-Calling Datasets | 该文使用多种 base model 作为 data labeler，输入端是可执行 API 描述、seed QA 示例与 prompt 模板，而不是已有交互轨迹。[Liu24n] 生成器据此构造自然语言 query 与对应 function call answer，再经过 format、execution 与 semantic 三阶段验证，只保留可执行且语义匹配的样本。[Liu24n] 最终 artifact 是标准化 JSON 实例，含 query、tools 与 answers；验证器 reasoning 主要用于内部筛选，未见被强调为下游复用的独立监督字段。[Liu24n] 数据集被发布为 xlam-function-calling-60k，用于 function-calling 模型微调。 | 属于 P7 | Policy(行为外化) | 构造式合成 | 结构化 query-tools-answers function-calling 数据，用于 xLAM 类模型 SFT。 | tool-use / function-calling | 检验一通过：主体来自模型基于 API spec 的新样本生成。[Liu24n] 检验二通过：数据集独立发布并供下游模型消费。[Liu24n] 检验三通过：主要产物是 tokenized carrier。[Liu24n] 交互性较弱，更接近单步 function-calling 数据；目标载体含强 Schematic 成分。 |
| [Liu24o] | ToolACE: Winning the Points of LLM Function Calling | 该文用 frontier LLM 从 API 文档中抽取并演化 API 定义，再让 user、assistant、tool 三类 agent 进行 role-play 式对话生成，输入端是 API 相关原始文档而不是现成轨迹。[Liu24o] 生成过程先做 Tool Self-Evolution Synthesis 扩展 API pool，再做 Self-Guided Dialog Generation，并用 learner loss 反馈调节样本复杂度。[Liu24o] 产物包括大规模 API pool 与多轮 function-calling dialogue，内含结构化 function call；Dual-Layer Verification 用规则和模型双层检查 hallucination、格式与一致性。[Liu24o] 最终数据作为独立训练资源，用于 ToolACE-8B 等模型的 SFT。 | 属于 P7 | Policy(行为外化) | 模拟环境 | API 自演化后的多轮 function-calling dialog 与结构化调用数据，用于 tool-use SFT。 | tool-use / function-calling | 检验一通过：主体来自模型根据 API 文档自演化并模拟对话生成。[Liu24o] 检验二通过：数据被物化为独立训练资源。[Liu24o] 检验三通过：主要产物是 carrier。[Liu24o] 产物同时含 Narrative 对话与 Schematic API 定义，Schematic 成分较强。 |
| [Fai26] | AutoSurfer -- Teaching Web Agents through Comprehensive Surfing, Learning, and Modeling | 该文用 GPT-4.1 作为 exploration、task synthesis 与 refinement teacher，输入端是 seed URL、页面 screenshot、简化 HTML、A11y tree 与行动历史，而不是输入 demonstration。[Fai26] 它通过 breadth-first 的系统化网页漫游发现可行动页面与状态，再基于探索中获得的页面关联生成复杂任务，并把任务元组转成带 reasoning 的 SFT 样本。[Fai26] 产物是 website-specific 的 web SFT dataset，含任务描述、页面视觉与结构信息、有限历史动作以及下一步 action 和 reasoning；质量控制包括任务筛选、打分阈值、trajectory refinement 与失败样本丢弃。[Fai26] 这些样本被用于训练特定网站的 wLLM。 | 属于 P7 | Policy(行为外化) | 真实环境锚定 | 真实网页探索后整理出的 SFT web trajectory tuple，用于网站专属 web agent 训练。 | web | 检验一通过：探索轨迹由 teacher 自行生成，后续产物不是对外部现成轨迹做抽象。[Fai26] 检验二通过：结果被整理为独立 Web SFT dataset。[Fai26] 检验三通过：主要产物是 carrier。[Fai26] 与 Explorer 相比更强调系统遍历与网站覆盖。 |
| [He25g] | Scalable Data Synthesis for Computer Use Agents with Step-Level Filtering | 该文用 OpenAI CUA 作为 teacher 生成 noisy rollout，再用 o4-mini 作为 grading model 对每一步动作打 0 到 10 分，输入端是真实网站任务、截图与轨迹上下文。[He25g] teacher 在真实网页中生成长轨迹后，系统先做 trajectory-level success 过滤，再做 step-level grading，并把高分步作为训练目标、低分步保留在上下文中形成 masked SFT 信号。[He25g] 产物同时包含 WebSTAR trajectory dataset、WebSCORE graded step 数据以及由这些标注蒸馏得到的 StepRM；judge 的 step score 与 reasoning 被明确保留下来并用于后续训练。[He25g] 这些 artifact 被用于 CUA student 的 SFT 以及过程奖励模型训练。 | 属于 P7 | 兼有 | 真实环境锚定 | 真实网页轨迹加 step-level grading 与 reasoning supervision，用于 CUA SFT 与 reward model 训练。 | web | 检验一通过：行为数据来自 teacher rollout，评估数据来自 grader 对这些 rollout 的判断能力外化。[He25g] 检验二通过：WebSTAR 与 WebSCORE 均是独立可复用数据资源。[He25g] 检验三通过：主要产物是 trajectory 与 label carrier。[He25g] 这是较强的“兼有”例子，Evaluator 外化不是单纯过滤开关。 |
| [Wei26b] | ANCHOR: Branch-Point Data Generation for GUI Agents | 该文以 GPT-5.1 做 branch-point identification 与新任务提议，Claude Sonnet 4.5 做执行 agent，Qwen3-VL-32B 做 step filtering 与 verification；输入端是少量高质量 seed demonstration。[Wei26b] 方法沿 seed trajectory 找到 branch point，在这些状态上让模型提出新任务并继续执行，形成共享前缀、后段分叉的树状 GUI 数据。[Wei26b] 最终保存的是成功 branched trajectory、截图、任务描述与 reasoning，并通过 trajectory-level verification、task-conditioned prefix 过滤与 post-branch 去噪做质量控制。[Wei26b] 数据被整理为公开训练集，用于 GUI agent 的 SFT。 | 边界 | Policy(行为外化) | 真实环境锚定 | 从 seed GUI demonstration 分叉扩展出的 branched trajectory 数据，用于 GUI agent SFT。 | GUI / mobile | 检验一边界：方法显著依赖已有 seed trajectory 作为分叉基底，和 P1/P2 的“基于现成轨迹再变形”有接壤。[Wei26b] 检验二通过：产物被物化为独立数据集。[Wei26b] 检验三通过：主要产物仍是 trajectory carrier。[Wei26b] 虽然后缀分支由模型新生成，但共享前缀占据关键输入地位，适合放边界。 |
| [Ver25b] | FABRIC: Framework for Agent-Based Realistic Intelligence Creation | 该文用 LLM 扮演 user proxy 与 agent，输入端仅需 enterprise domain 与 natural-language use case 等轻量 seed，而不是已有 agent trajectory。[Ver25b] 它逐级合成 scenario、function inventory、policy pseudocode、user scenario pack 与 agentic execution record，形成从任务到工具到执行的离线合成链条。[Ver25b] 产物包含自然语言对话、JSON schema、Python pseudocode 与可验证执行序列，并经过 schema enforcement、参数类型检查、逻辑一致性检查和 LLM 过滤。[Ver25b] 最终 agentic record 被序列化为训练数据，用于 tool-use agent 的 SFT 与鲁棒性评测。 | 属于 P7 | Policy(行为外化) | 构造式合成 | 结构化 scenario-function-pseudocode-execution 数据，用于 agentic tool-use SFT。 | tool-use / function-calling | 检验一通过：主体由模型从极少 seed 构造，不依赖输入轨迹。[Ver25b] 检验二通过：AgenticRecordRollout 物化出独立数据资源。[Ver25b] 检验三通过：主要产物是可训练 carrier，不是交互 arena。[Ver25b] 目标载体含很强 Schematic Tokenized 成分，尤其是 pseudocode 与 schema。 |
| [Xie25e] | AgentSynth: Scalable Task Generation for Generalist Computer-Use Agents | 该文用 GPT-4.1 与 GPT-4.1-mini 分别承担 task proposer、executor、verifier、reviser 与 summarizer 等角色，输入端是 persona、桌面初始截图与执行历史，而不是现成 demonstration。[Xie25e] 它先生成一串可验证的简单 subtasks 并执行、验证，再把这些子任务总结成更长程的复合任务，同时保留相应的 action trajectory、reasoning trace 与视觉证据。[Xie25e] 质量控制包括人工抽查任务可行性与 realism、自动 verifier 校准、对 near-miss 情况的鲁棒性测试，以及 success label 和 completion percentage 标注。[Xie25e] 最终形成 6000+ task 与 trajectory 数据，用于 generalist computer-use agent 的训练与评测。 | 属于 P7 | Policy(行为外化) | 真实环境锚定 | OSWorld 等环境中的 task-trajectory 数据，含 reasoning 与视觉证据，用于 CUA 训练与评测。 | GUI / mobile | 检验一通过：任务和轨迹来自模型在环境中的生成与执行，而非输入轨迹改写。[Xie25e] 检验二通过：数据被物化为独立数据集。[Xie25e] 检验三通过：主要产物是 task 与 trajectory carrier。[Xie25e] 论文也强调 benchmark 价值，但主实验里训练数据地位足够实。 |
| [Tia26] | ASTRA: Automated Synthesis of agentic Trajectories and Reinforcement Arenas | 该文用 GLM-4.6-FP8 从 tool document 与知识源出发，同时生成 SFT trajectory 和 RL arena，输入端不是已有 agent trajectory。[Tia26] 一条线构造 tool-chain、任务与多轮交互样本，另一条线把 QA 对与工具实现编译成可沙箱验证的 Python 环境，供后续 RL 使用。[Tia26] 产物既有 multi-turn tool-use trajectory，也有 tool specification、tool implementation 与 reward-verifiable arena；前者用多维 LLM 打分过滤，后者用 sandbox correctness 检验。[Tia26] 这些结果被联合用于两阶段训练：先 SFT 后在线 RL。 | 边界 | Policy(行为外化) | 模拟环境 | 同时产出 tool-use trajectory 数据与可执行 reinforcement arena，用于 SFT 与 RL。 | tool-use / function-calling | 检验一通过：主体由模型基于 tool doc 与知识源生成，不是对输入轨迹改写。[Tia26] 检验二通过：SFT 数据与 RL 环境都被物化并供后续系统消费。[Tia26] 检验三边界：trajectory 是合法 carrier，但 reinforcement arena 也是论文实质主产物。[Tia26] 若本节只写 carrier，可把 arena 部分单独拿出做边界说明。 |
| [Lu26j] | Mock Worlds, Real Skills: Building Small Agentic Language Models with Synthetic Tasks, Simulated Environments, and Rubric-Based Rewards | 该文用 Qwen3-235B teacher 从 persona 出发生成 workflow、任务与虚拟 tool ecosystem，再用较小 Qwen3-30B 扮演 mock user 和 mock tool，构成稳定的模拟环境。[Lu26j] 输入端是 persona 等 seed 信息，不是现成轨迹；生成结果同时含 fuzzy task、隐藏用户上下文、虚拟工具定义、task rubric 与用于复现一致响应的映射结构。[Lu26j] 质量控制依赖 teacher 轨迹能否可靠执行工作流、reward 稳定性分析与 rubric-based judge；训练时主要用这些模拟世界与 rubric 奖励做 RL。[Lu26j] 论文会发布 synthetic dataset，但整体框架强调的是闭环 mock world 与 reward 体系。 | 边界 | 兼有 | 模拟环境 | 模拟 tool-use 任务、虚拟工具、rubric 与环境映射，主要用于 agentic RL。 | tool-use / function-calling | 检验一通过：主体来自模型从 persona 与 workflow 能力出发构造，不依赖输入轨迹。[Lu26j] 检验二边界：虽然有可发布数据，但核心消费方式是同一闭环 RL 系统；可迁移数据资产地位次于环境与奖励框架。[Lu26j] 检验三边界：主要产物同时包括 environment 与 rubric，而不只是 carrier。[Lu26j] Policy 与 Evaluator 产物兼有，但环境生成是全篇最突出的部分。目标载体含明显 Schematic 成分。 |
| [Xu26e] | Controllable and Verifiable Tool-Use Data Synthesis for Agentic Reinforcement Learning | 该文把 LLM generator 与轻量 judge 结合成两阶段流程，输入端含少量人工 seed trajectory、tool schema 与多样性指令。[Xu26e] 第一阶段做 self-evolving generation 形成可靠 base instance，第二阶段在保留 oracle tool call 与 answer 的前提下，系统性地扰动 user query、tool set 与 tool output，构造 RL-ready mock tool 环境。[Xu26e] 保存的 artifact 是 \((s,u,\mathcal{T},c^*,o^*,a^*)\) 元组及 verifier metadata，验证由格式、规则、语义检查和 capability-conditioned reward 组成。[Xu26e] 这些结果主要被用于 GRPO 式 agentic RL，而不是以独立静态监督集的形式单独展开。 | 边界 | 兼有 | 模拟环境 | 带 oracle 与 verifier metadata 的 tool-use base instance 及其增强环境，用于 agentic RL。 | tool-use / function-calling | 检验一边界：主体并非直接重写 seed trajectory，但 seed trajectory 明确参与自举起点。[Xu26e] 检验二边界：数据被物化，但主要服务同一 RL 管线，带有较强 self-training 耦合。[Xu26e] 检验三边界：论文主结果是 RL-ready augmented environment，而非纯 carrier。[Xu26e] Evaluator metadata 是实质产物之一；若正文只收更“静态”的 token artifact，可列边界。 |
| [Yan25x] | Step-GUI Technical Report | 该文让当前轮 policy model 在 GUI 环境中 rollout，再由更强的 thinking model 通过 Calibrated Step Reward System 对轨迹进行多维抽取与重写，输入端包括任务指令、截图与当前 policy 的执行轨迹。[Yan25x] CSRS 为每一步生成 progress tracking、state summary、effect prediction、self-reflection、state verification、intent execution 与 action prediction 等七类结构化标注，同时保留高质量 CoT。[Yan25x] 这些 artifact 被分成 knowledge data 与 trajectory data，并用于下一轮 cold-start fine-tuning 与 RLVR；环境覆盖 Android、Ubuntu、Windows 与 macOS 等真实系统或其虚拟化实例。[Yan25x] 论文强调的是持续自演化训练飞轮，而不是单次独立数据集发布。 | 边界 | 兼有 | 真实环境锚定 | GUI rollout 轨迹及七类 step-level 过程标注，用于下一轮 GUI agent 训练。 | GUI / mobile | 检验一通过：评估外化部分来自 thinking model 的判断能力，对已有 rollout 做多维标注并不等同 P1/P2 式简单重表述。[Yan25x] 检验二边界：产物主要回流到同一家族后继模型，self-training 耦合很强。[Yan25x] 检验三通过：主要产物仍是 trajectory 与 annotation carrier，不是环境。[Yan25x] 这是典型的“有 P7 成分，但更接近 composite flywheel”的边界样本。 |
| [Log26b] | Scaling Web Agent Training through Automatic Data Generation and Fine-grained Evaluation | 该文用 GPT-4 生成任务、Llama 3 系列 teacher 生成网页交互轨迹，并用 Llama 3.3 70B 与 Gemma 3 27B judge 做 fine-grained constraint evaluation，输入端是网站页面状态和合成任务，而不是已有 demonstration。[Log26b] teacher 在真实网站执行后，系统按 constraint satisfaction rate 对轨迹做细粒度分析，从部分成功轨迹中提取最优 prefix，并对未完全达成的轨迹做 hindsight task relabeling。[Log26b] 保存的 artifact 是带 URL、A11y tree、screenshot、action 与 CoT 的 web trajectory prefix；judge 的细粒度约束判断主要用于 prefix 提取与过滤，而非作为独立监督数据集单列发布。[Log26b] 最终约 6.5 万条可用轨迹被整理为训练集，用于 student web agent 的蒸馏式 SFT。 | 属于 P7 | Policy(行为外化) | 真实环境锚定 | 真实网页 trajectory prefix 与 relabeled task 数据，用于 web agent distillation。 | web | 检验一通过：主体是 teacher 在真实网站上的新 rollout；后续 prefix extraction 与 relabeling 属于生成后整理，不是对输入外部轨迹转写。[Log26b] 检验二通过：产物被物化为独立训练集供 student 使用。[Log26b] 检验三通过：主要产物是 trajectory carrier。[Log26b] judge 输出更像质量控制与 hindsight 重标，不宜单列为评估外化。 |