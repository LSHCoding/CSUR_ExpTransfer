
| 引用标识 | 标题 | 关键思想与方法 | 语义功能 | 轨迹来源 | 下游消费机制 | 领域 | 备注 |
|---|---|---|---|---|---|---|---|
| [Wan23c] | Voyager: An Open-Ended Embodied Agent with Large Language Models | 该工作从 agent 在 Minecraft 中完成任务时产生的成功执行经验出发，经验来源是 agent 自身持续 practice 的 rollout，并伴随环境反馈、代码执行报错与 self-verification 结果。方法上，它用 iterative prompting 让 GPT-4 基于任务尝试过程反复生成、执行、修正 action program，直到程序在环境中通过验证后才写入 skill library。产物是可执行的 Mineflayer JavaScript code skill，库中以 skill description 的 embedding 为索引，值为可执行程序本体。下游时，系统按当前 task plan 与环境反馈检索 top-k skills，把这些程序放入 prompt 供模型组合调用，以完成更复杂的 embodied 任务；论文写明库会持续增长，但未给出显式 dedup 或 versioning 机制。 | skill | self | 检索到的 code skill 被放入 prompt，并作为高层可调用程序由运行时执行。 | embodied / robotics | 属于较干净的 P2 skill 案例：输入是自我交互轨迹，输出是可执行代码 skill，后续依赖其可调用结构复用。 |
| [Zhe25c] | SkillWeaver: Web Agents can Self-Improve by Discovering and Honing Skills | 该工作以 web agent 在网站上自主探索时产生的 practice trajectories 为输入，轨迹由 agent 自己在自动课程驱动下完成网站功能发现与尝试得到。方法上，它先用 reward model 从练习轨迹中识别成功行为，再把整段 state-action 序列串化后提示 LLM 生成泛化的 Python API，并在 honing 阶段用自动化 unit test、静态分析与环境反馈继续调试。产物是带 typed parameters 的 Playwright skill function，包含函数签名、docstring、usage log、prerequisite state 和可执行代码体。下游时，这些 API 被加入 action space 作为高层技能调用，并可进一步组合成 compositional APIs；系统还带有 API selection 模块按前置条件与当前页面过滤候选 skills。 | skill | self | skill 作为高层 API 加入 action space，由 agent 直接调用并由浏览器运行时执行。 | web | 也是较干净的 P2 skill。虽然 skill 带 docstring，但核心复用单位是可调用程序而非自由文本。 |
| [Wan26d] | WebXSkill: Skill Learning for Autonomous Web Agents | 该工作使用具体 web execution trajectories 作为输入，但这些轨迹来自更强 teacher model 在 WebArena 与 WebVoyager 上合成并执行的 synthetic traces，而非人工示范。方法上，它从轨迹中抽取可复用 action subsequence，把实例值抽象成 typed parameters，并为每一步补写 natural-language guidance；随后再按 URL pattern 把技能组织进 URL-based skill graph，并通过相似度与环境验证做去重和更新。产物是 JSON 形式的参数化 skill，含 semantic signature 与一串 action steps，每步都带 action type、element reference、参数槽位和 guidance。下游时，skill 既可在 grounded mode 下作为 atomic tool call 由 runtime 执行整段动作，也可在 guided mode 下作为分步说明注入 prompt 供 agent 按步骤执行。 | skill | teacher | skill 可被上下文检索后作为原子调用直接执行，或以分步 guidance 注入 prompt 供 agent 依步骤复用。 | web | 判为 skill 而非 workflow：虽然内部有 step sequence，但整体被当作一个可调用技能单元检索和使用。source-side 成立，因为输入仍是具体执行轨迹，只是来源于 teacher 合成执行。 |
| [Wan25d] | Inducing Programmatic Skills for Agentic Tasks | 该工作从 agent 在线求解任务时得到的正确 episodes 出发，先用 evaluator 过滤出被判为成功的轨迹，再对轨迹做清洗，去掉报错步骤并压缩冗长 thought。方法上，induction module 直接从成功子轨迹诱导一个或多个 Python functions，并把原始轨迹重写为使用新 skill 的 trajectory，再通过 re-execution 检验 skill 是否真的被调用、是否导致环境变化、是否仍能成功完成任务。产物是带函数名、typed arguments、docstring 与程序体的 executable Python skills，程序体可包含 primitive actions、旧技能调用以及 if-else 或 loop 等控制结构。下游时，这些 skill 进入 agent action space，模型在推理时可像调用高层动作那样调用它们完成多步 web 操作；论文还描述了在新网站上快速 refine 或重建旧技能的过程。 | skill | self | 诱导出的 Python skill 被加入 action space，agent 在推理时直接调用，由执行器运行其多步程序体。 | web | 干净的 P2 skill。其关键点是从成功轨迹中抽取并程序化成 callable skill，再用重写轨迹和再执行验证。 |
| [Lee23] | MobileGPT: Augmenting LLM with Human-like App Memory for Mobile Task Automation | 该工作从 mobile 环境中的 app exploration 与 user traces 中收集交互经验，来源同时包括随机探索器和 user trace monitor 记录的实际操作过程。方法上，它通过 Explore-Select-Derive 流程把已完成任务转写成高层 task、sub-task 与 primitive actions 的层级表示，并把页面与子任务关系组织为 transition graph；相似 screen 还会通过功能相似度映射到已有节点。产物是三层 hierarchical memory，与一个以 app pages 为节点、以 sub-tasks 为边的图结构共同组成，其中边本身承载执行该子任务所需的 action sequence。下游时，系统在接到新指令后先做 recall，若匹配到已有记忆，就直接按 memory 中的 task-subtask-action 结构执行，并通过 attribute-based 或 in-context adaptation 适配参数和页面变化；人工修复后的轨迹也会被回写进 memory。 | graph | mixed | 系统检索匹配的 task 和 transition path，直接按其中存储的 sub-task 与 action 序列执行。 | GUI / mobile | 判为 graph：核心组织形式是页面节点与子任务边的转移图，而非单个 callable skill。它同时带有流程信息，和 workflow 子节有一定交叉。 |
| [Liu25i] | Beyond Training: Enabling Self-Evolution of Agents with MOBIMEM | 该工作从 past task executions、OS metadata、screenshots、UI hierarchy 以及用户纠错事件中积累经验，来源同时包含 agent 执行历史与人类介入。方法上，它把原始经验分别转成三类结构：Profile Memory 的 DisGraph、Experience Memory 的 multi-level templates，以及 Action Memory 的 ActTree 和 ActChain；用户中断时还会分析完整执行轨迹与纠正动作，提炼出改进模板。产物中，高层 experience template 编码任务级 control flow，低层 template 保存具体 UI steps，ActTree 以 UI states 为节点并缓存 prefix paths，ActChain 把详细 action sequence 绑定到具体模板。下游时，Task Rewriter 会为新任务匹配并填充 experience template 的槽位生成可执行方案，AgentRR 会在 ActTree 或 ActChain 中匹配当前 UI 状态后直接重放动作；系统还支持节点拆分、路径合并、UI 校验失败后的回退与更新。 | workflow+graph | mixed | experience template 被匹配并实例化为执行方案，ActTree 与 ActChain 被状态匹配后直接重放动作序列。 | GUI / mobile | 组合值。workflow 部分是 multi-level experience templates，graph 部分是 DisGraph 与 ActTree；若只取本小节最核心内容，重心更偏 workflow memory。 |
| [Wei26] | WorkflowGen:an adaptive workflow generation mechanism driven by trajectory experience | 该工作从历史执行日志中提取经验，日志同时记录 task planning、tool invocation、模型反馈、成功路径以及异常与回滚信息。方法上，它在 node level 与 workflow level 两个粒度做经验抽取，把具体 trajectory 中的可变节点抽象出来，并将结构相同的轨迹聚类合并成 generalized workflow templates。产物是带拓扑结构的 workflow representation，节点包含 tool call、依赖关系、contextual data 以及 fixed versus generatable 标记，整体可呈现顺序、分支或并行结构。下游时，系统按 query 与历史 trigger 的相似度做三级路由：高相似时直接复用原 workflow，中等相似时只重写变量节点，低相似时重新规划；其 repository 还负责模板聚类、去重、聚合和高频模板优先级提升。 | workflow | self | 检索到的 workflow 被直接执行，或在保留结构的前提下只重写变量节点后执行。 | general / tool-use | 属于较干净的 P2 workflow。 |
| [Liu26] | FlowMind: Execute-Summarize for Structured Workflow Generation from LLM Reasoning | 该工作先让模型借助工具完成任务并记录 execution trajectory，轨迹中包含 tool calls、intermediate states 和 final results，然后再单独进入 summarize 阶段。方法上，系统不直接从任务描述生成流程，而是用 graph-building tools 从 recorded execution traces 中抽取 essential steps、dependencies 与 control flow，构成带 sequencing、branching 和 iteration 的结构化工作流。产物是节点和边显式化的 workflow graph，节点可表示 tool invocation、条件或开始结束标记，边编码依赖与执行顺序。下游时，这些 workflow 被用于复现、调试、审计和后续自动化系统调用，构建过程中还带 graph-state feedback 与 function-call error recovery 以修正生成过程。 | workflow | self | 生成的 workflow 作为结构化工件供复现、调试、审计和后续自动化系统调用。 | general / tool-use | source-side 成立，但 hard standard 3 较弱：论文更强调 workflow reconstruction 与 downstream automation，而不是持久化 memory library 中被 agent 持续检索复用。若本节只收“经验转成可持续复用的 workflow memory”，它是边界案例。 |
| [Tan26] | Enhancing Web Agents with a Hierarchical Memory Tree | 该工作从成功 web trajectories 构造记忆树，轨迹形式为自然语言指令与 observation-action 序列；论文同时讨论了离线用现成成功轨迹建树和在线成功后增量插入两种 setting。方法上，系统先做 instruction normalization，再把轨迹切成语义连贯的 subgoal segments，并为每段生成显式 pre-condition、post-condition 与语义化 step abstraction；原始 DOM 标识则被替换成可迁移的 semantic element description。产物是三层 rooted tree，根层为 intent 与 constraints，中层为带 pre/postcondition 的 stage，叶层为 action pattern 与目标元素语义描述。下游时，Planner 依据当前 observation 与 stage 的 pre/postcondition 对齐当前所处流程阶段，Actor 再在该阶段下取 step exemplars 并将抽象动作 ground 到当前页面元素；若阶段选择置信度低则回退到 baseline policy。 | workflow | mixed | Planner 依据树中的 stage 结构选阶段，Actor 再依据对应 action pattern 在当前页面执行。 | web | 判为 workflow。虽然树节点也用状态条件做对齐，但核心用途是分阶段组织任务执行逻辑。离线建树的数据来源在论文里可能是现成成功轨迹，因而来源记为 mixed。 |
| [For25] | Learning Hierarchical Procedural Memory for LLM Agents through Bayesian Selection and Contrastive Refinement | 该工作以环境交互得到的 episodic trajectories 为输入，轨迹由 observation、primitive actions 与 reward 构成，并在 ALFWorld、WebShop、TravelPlanner 和 InterCodeSQL 等环境中积累。方法上，它先把轨迹分段为语义一致的 sub-task，再把每段抽象成带 goal、precondition、action sequence 和 postcondition 的 atomic procedure；多段 procedure 若反复共同带来成功，则进一步抽象为带 conditional transition policy 的 meta-procedure。产物是两层 hierarchical procedural memory，其中 atomic procedure 是四元组，meta-procedure 则显式保存 procedure sequence 与 continue、skip、repeat、abort 等控制逻辑。下游时，agent 在运行时先检索候选 procedures，再用 Bayesian expected utility 排序选择最合适的 procedure 或 meta-procedure 执行；系统还会在成功失败积累后触发 contrastive refinement，收紧条件、修补动作并按 reliability、frequency、recency 做 pruning。 | workflow | self | 检索到的 procedure 或 meta-procedure 经 Bayesian selection 选中后作为执行流程被 agent 按阶段使用。 | general | 尽管论文使用 procedure 一词而非 workflow，但其工件明确编码多步过程、前后置条件与可选控制流，按你的口径应归 workflow。 |
| [Fen26] | Environment Maps: Structured Environmental Representations for Long-Horizon Agents | 该工作从 screen recording、browser trace、DOM snapshot 与 screenshot 中提取 multimodal interaction records，实验上主要使用 WebArena 发布的人类轨迹数据。方法上，它先把原始事件解析成 unified step sequence，再抽取 taken actions 与 potential actions，对动作做参数化 generalization，并按规范化 URL pattern 形成 context；之后再抽取 tacit knowledge 并把相同 context 与重复知识项合并。产物是 JSON 形式的 environment map，包含 context nodes、parameterized action nodes、workflow order、alternative relations 和 tacit knowledge，其中 node 与 edge 同时编码页面结构、可行动作及常见流程顺序。下游时，agent 通过 Read、Grep、Glob 等文件工具查询 map 中的环境结构、可用动作和程序性知识，把检索到的结果作为 planning 支持；系统支持增量更新、人工编辑和 deterministic merging。 | workflow+graph | human | agent 通过文件查询接口检索 map 中的 context、actions、workflows 与 tacit knowledge，再据此规划执行。 | web | 组合值。若只取主语义功能，更偏 graph，因为核心是环境与可行动作的结构化表示；但其 workflows 字段和 sequential edges 也真实编码流程顺序。 |
| [Wan24] | Agent Workflow Memory | 该工作把 web agent 的 canonical experiences 转成可复用 workflow，离线 setting 下输入可来自人类标注或模型合成的成功经验，在线 setting 下输入来自 agent 自己经 evaluator 判为正确的 past predictions。方法上，induction module 从一条或多条经验中抽取常见 sub-routines，把实例值替换为占位符，并在更细粒度的 recurring subtask 上归纳 workflow。产物由 workflow description 与 workflow trajectory 组成，后者是一串步骤，每步都带当前环境状态的自然语言描述、决策 reasoning 与可执行 action program。下游时，workflow 一种用法是直接追加到系统 memory 中供 agent 参考，另一种用法是把 workflow 包装成高层函数加入 action space；系统还按 website 维持分站点 workflow 集合，并在 online 场景中只对 evaluator 判正的轨迹做持续归纳。 | workflow | mixed | workflow 被追加到 memory prompt 供 agent 依流程参考，或被包装成高层函数加入 action space。 | web | 这是你定义下很关键的自然语言 workflow 案例：虽然承载形式是文本，但步骤骨架明确，且下游确实依该结构复用。 |
| [Mi26] | Skill-Pro: Learning Reusable Skills from Experience via Non-Parametric PPO for LLM Agents | 该工作从 agent 的 post-episode trajectories 出发，输入是状态、动作、奖励构成的 interaction traces，来源是 agent 在 ALFWorld 与 TextArena 等环境中的自我交互。方法上，它通过 hindsight attribution 从成功或失败轨迹中抽取 semantic gradients，聚合后提出新的 skill 候选，再用 PPO-style trust-region gate 检查候选 skill 在历史轨迹上的 counterfactual advantage 是否足够。产物是显式自然语言 skill，形式为 activation condition、execution procedure 与 termination condition 三元组，作为 temporally extended procedural unit 保存。下游时，agent 在 Skill-MDP 中先选 skill，再由冻结 LLM 在该 skill 的 execution procedure 条件下生成 primitive actions；系统还用 similarity 或 value 做 skill selection，并按分数、冗余与容量约束做 pruning。 | skill | self | 先选中一个 skill，再由 LLM 在该 skill 的 procedure 约束下生成并执行 primitive actions。 | general | 判为 skill。虽然工件是自然语言而非代码，但它是一个整体可选、可启停、可执行的动作单元，而不是整张任务流程图。 |
| [Ano24] | AriGraph: Learning Knowledge Graph World Models with Episodic Memory for LLM Agents | 该工作从 text game 中的具体环境交互构图，输入包含每步 textual observation、action 与 reward，图在交互过程中从零开始持续更新。方法上，系统用 LLM 从 observation 抽取 semantic triplets，把对象及其关系加入 semantic memory，同时把每个时间步的完整 observation 作为 episodic vertex，并把该 observation 与当步抽取出的 triplets 连接起来。产物是由 semantic graph 与 episodic graph 共同组成的 world model，其中 semantic nodes 表示对象或实体，semantic edges 表示事实关系，episodic nodes 表示具体时刻的 observation，episodic edges 把观测与相关 triplets 绑定。下游时，agent 先做 semantic search，再沿 episodic links 找相关 past observations，把检索结果写入 working memory 供规划模块与 ReAct 决策模块使用；系统也会删除与新观测冲突的旧关系。 | graph | self | 先对 memory graph 做检索与遍历，再把相关 triplets 和 episodic observations 写入 working memory 供规划与行动。 | reasoning / text-game | 干净的 graph 案例。核心是关系结构与状态记忆，不是 workflow。 |
| [Zha25] | G-Memory: Tracing Hierarchical Memory for Multi-Agent Systems | 该工作从多 agent 协作中的细粒度 communication logs 出发，输入是带 speaker 与文本内容的 atomic utterances，以及任务结果信号。方法上，它先把 utterances 组织成 interaction graph，再把整次 query 与其 interaction graph 实例化为 query node，最后由 LLM 对交互与结果做总结，形成 insight nodes 并与支持它们的 queries 建立关系。产物是三层 memory graph，包括 utterance-level interaction graph、query graph 和 insight graph，边分别编码时序、启发、语义关联和 insight 支撑关系。下游时，新 query 到来后系统做上下双向 traversal：向上取高层 insight，向下取稀疏化的成功协作片段，并按不同 agent 的角色把这些内容裁剪后写入各自上下文；系统还会持续更新 supporting query sets 并做 graph sparsification。 | graph | self | 对三层 memory graph 做双向遍历后，将筛选出的 insights 与 interaction snippets 注入各 agent 上下文。 | multi-agent | 判为 graph 而非 workflow：其结构主要编码记忆关系和协作经验的层次联系，而不是任务步骤调度图。 |
| [Ma26] | BrainMem: Brain-Inspired Evolving Memory for Embodied Agent Task Planning | 该工作从 embodied agent 的连续 interaction histories 与 robot states 中提取经验，覆盖成功与失败轨迹。方法上，系统在 step level 把 action、feedback、state、location 等写入 working memory 并更新 episodic knowledge graphs，在 episode level 由独立 Experience Agent 对成功轨迹提炼 generalizable patterns，对失败轨迹通过 guided retry 分析失败原因后再压缩为 symbolic guidelines。产物是三层 memory：短期 textual working memory，episodic dual KGs，其中 Trajectory KG 记录 action-state transitions，Spatial KG 记录 room-object 等空间关系，以及一组可复用 semantic guidelines。下游时，系统按任务检索 working、episodic 与 semantic 三层信息，并把相关内容序列化为文本提示注入 planner；同时以 utility score、时间衰减和 pruning 机制持续维护 semantic memory。 | workflow+graph | self | 检索到的 trajectory KG、spatial KG 与 semantic patterns 被序列化为文本提示注入 planner。 | embodied / robotics | 组合值。Trajectory KG 带明显过程结构，Spatial KG 是环境关系图；但 semantic guidelines 又带有 P1 成分，所以这篇有跨子节重复引用风险。 |
| [Tan25b] | Meta-Agent-Workflow: Streamlining Tool Usage in LLMs through Workflow Construction, Retrieval, and Refinement | 该工作以 tool utilization trajectories 为输入，轨迹覆盖从 user query 到 task completion 的完整 tool-calling path，并包含成功与失败场景；实验中主要使用 ToolBench 及 agent 的 DFS 搜索过程。方法上，它先把最优轨迹映射成一串 tool invocation templates，再用 SGLang 为每个节点实例化参数，最终拼接成 DSL workflow；若执行时报错，则再定位故障节点并由 LLM 生成 revision plan 重构对应 DSL blocks。产物是面向 Dify 平台的 JSON DSL workflow，节点保存工具配置、headers、method、URL 等调用信息，边定义执行顺序与逻辑连接。下游时，系统先用 embedding recall 加 LLM reranking 选取最合适的 workflow，再把该 DSL 上传并交给 Dify 平台执行；后续还可对失败节点局部修补。 | workflow | mixed | 检索选中的 DSL workflow 被上传到 Dify 平台，由平台按节点和边定义执行。 | general / tool-use | 属于 workflow，但来源侧有一定不纯：论文既用现成 ToolBench 轨迹，也讨论 agent 生成轨迹。若你要最严格区分 self 与 teacher，这篇可进一步核对 ToolBench 轨迹的具体生成方式。 |
| [Shi26] | Evolving Programmatic Skill Networks | 该工作记录 agent 在环境中执行技能时产生的 concrete execution traces，trace 中包含被调用 skill、执行前后符号状态与执行结果，来源是 agent 自身持续交互。方法上，系统一方面在现有网络无法满足目标时用 planner 和 CODEGEN 生成新 symbolic skill，另一方面持续利用执行 traces 做 failure credit assignment、局部补丁、冗余合并与公共子技能抽取。产物是一个 directed compositional skill network，其中每个 skill 都含 executable control flow、参数、precondition、postcondition 与 CHILDREN 调用关系。下游时，agent 在网络上做 backward-chaining，按后置条件匹配可复用 skill，再由 PSN Manager 执行选中的程序并继续记录新 trace；系统还带有 maturity-aware update gating 和在线 structural refactoring。 | skill+workflow | self | agent 在 skill network 上做 backward-chaining 规划，选中的 programmatic skill 由执行器直接运行。 | embodied | 按你当前口径可纳入 P2。它不是“从轨迹直接一次性蒸馏出 artifact”的最纯案例，但 execution trace 确实持续驱动 skill network 的形成、修补与重构；若正文要分层，可注明其是“trace-driven evolution”型。 |
| [Qiu26] | AutoRefine: From Trajectories to Reusable Expertise for Continual LLM Agent Refinement | 该工作以 agent 在 ALFWorld、ScienceWorld 与 TravelPlanner 中自生成的 success and failure trajectories 为输入，并按批次做对比式抽取。方法上，extraction agent 跨成功与失败轨迹比较 recurring action sequences 与 causal principles，把得到的经验模式分成 Skill Patterns 与 Subagent Patterns，再配合 embedding-based merging 和 utility-based pruning 维护模式库。产物中，Skill Pattern 可表现为带步骤的 natural-language procedural guideline 或 executable code snippet，Subagent Pattern 则是带独立 system prompt、reasoning loop 与内部 memory 的 specialized subagent。下游时，系统对新任务做 multi-query retrieval，text skill 通过 prompt augmentation 使用，code skill 通过 tool registration 使用，subagent pattern 则通过 hierarchical delegation 处理复杂子任务。 | skill+workflow | self | 检索到的 pattern 以 prompt augmentation、tool registration 或 hierarchical delegation 的方式被主 agent 使用。 | general | 按你当前口径可作为 P2。其双形态产物带明显 composite 性：skill pattern 更接近 skill，subagent pattern 更接近 workflow。正文中可提示它跨子功能。 |
| [Xu25b] | A-MEM: Agentic Memory for LLM Agents | 该工作从 agent 与环境交互得到的 raw interaction content 出发，把每次经验转成结构化 memory note，并附上时间信息。方法上，LLM 为每条经验生成 keywords、tags、contextual description 与 embedding，再通过 link generation 在新旧 notes 之间建立语义关系；后续新经验还可触发对旧 note 的 context、keywords 与 tags 的重写。产物是带字段集合 \(c_i, t_i, K_i, G_i, X_i, e_i, L_i\) 的 atomic note，以及由 linked memories 和 boxes 形成的 interconnected memory network。下游时，系统先按 query embedding 检索 top-k notes，再自动扩展到同 box 内或已链接的相关 notes，把这组关系化记忆一起提供给 agent 做后续推理。 | graph | self | 先做向量检索，再沿 linked memories 和 boxes 扩展取回相关 notes 作为上下文。 | general | 按你当前口径可纳入 P2，并归到 graph。它不是流程结构，而是 relation structure。若正文区分 procedure 与 relation 两支，这篇应放 graph 支。 |
| [Jo26] | From Logs to Agents: Reconstructing High-Level Creative Workflows from Low-Level Raw System Traces | 该工作从 node-based 创意软件中的 raw system traces 出发，输入是 csv 或 JSON 形式的低层事件流，来源是人类用户在 Creativity Support Tools 中的真实操作。方法上，它先做 semantic filtering 和事件分类，去除系统噪声，再依据 parent-child metadata 重建设计序列，并把动作与资产类型标准化为 behavioral tokens。产物是描述 creative process 的 DAG 与 token sequence，其中节点表示资产和编辑状态，边表示数据流与分支演化。下游时，论文将重建出的 workflow 用于 Markov-style predictive analysis，并把 workflow history 提供给 process-aware agents 作为上下文，以支持 intent inference、next-step suggestion 和 rationale grounding。 | workflow | human | 重建出的 workflow DAG 被用于过程分析，并作为 process-aware agent 的上下文输入。 | GUI / creative tools | 按你当前口径可纳入 P2，而且是 human-log-to-workflow 的干净案例。若综述主线仍强调 LLM agent 本体，这篇更适合作为外延边界或 antecedent。 |
| [Zha25c] | A2Flow: Automating Agentic Workflow Generation via Self-Adaptive Abstraction Operators | 该工作从 expert demonstrations 和 task-resolution 数据出发，而不是从随机探索日志开始，输入是具体成功样例。方法上，它先把 case 级示范转成 code-based workflow 与初始 operators，再经 operator clustering 与 deep extraction 得到 task-aware abstract execution operators，并用 MCTS 在节点、边与 operator 组成的搜索空间中优化整体流程。产物是 directed workflow graph，节点表示带 model、prompt、temperature 与 output format 的 LLM operation，边表示执行顺序与控制逻辑，节点内部可调用抽取得到的 operators。下游时，系统直接执行该 workflow graph 解决 code、math、reading comprehension、ALFWorld 等任务，并通过 operators memory mechanism 把先前节点输出累计为后续节点的上下文。 | workflow | human | 搜索得到的 workflow graph 被直接执行，节点按边定义的控制逻辑串联运行。 | general | 按你当前口径可纳入 P2。它更像 demonstration-to-workflow induction，而不是自我交互 memory，但输入确是具体示范，输出也确是显式 workflow。 |
| [Xu24b] | In-Context Ensemble Learning from Pseudo Labels Improves Video-Language Models for Low-Level Workflow Understanding | 该工作以 human demonstration videos 为输入，数据来自 WONDERBREAD benchmark 中记录软件操作过程的 screen recordings。方法上，系统先用 multimodal in-context learning 从视频生成多个 pseudo SOP，再由最终模型结合原视频与这些 pseudo labels 聚合出一份最终 SOP。产物是按时间顺序组织的 step-by-step Standard Operating Procedure，通常呈现为编号步骤列表，明确描述动作的 chronological ordering。下游时，论文把该 SOP 视为自动化 business software workflows 的基础表示，用于 documentation generation 与后续 workflow automation 能力建设。 | workflow | human | 生成的 SOP 作为 step-ordered workflow text 被下游自动化或文档系统使用。 | GUI / video workflow understanding | 按你当前口径可纳入 P2。它是 human-video-to-SOP 的 clean workflow case，但更偏 workflow understanding 与 documentation，不是 agent memory 论文。 |
| [Cha20] | Workflow Graphs: A Computational Model of Collective Task Strategies for 3D Design Software | 该工作从 3D 建模任务中的具体用户 traces 出发，输入同时包括 timestamped command invocations、每步后的 3D model snapshots 与 time-aligned screen recordings，来源是人类用户在 instrumented Tinkercad 中完成固定建模任务的过程。方法上，系统先把每条 demonstration 初始化成从 START 到 END 的线性路径，再通过 preprocessing 合并冗余命令、用 DBSCAN 沿单条路径聚合相似中间状态，并进一步在不同用户路径之间用几何相似度合并 equivalent-intermediate nodes。产物是 W-graph，即一个 directed graph，节点表示 semantically meaningful task states 或 subgoals，边表示从一个状态到另一状态的 workflow path，并可保留多条不同 demonstrations 的替代路径。下游时，论文基于该图提出 W-Suggest、W-Guide 与 W-Instruct，用于给当前用户推荐更优路径、按当前状态展示后续示范、以及帮助教师分析群体策略与常见错误。 | workflow | human | 当前状态会被对齐到 W-graph 上，再据图中后续路径检索建议、示范或教学分析结果。 | GUI / 3D design | 重新看 PDF 后，这篇是很干净的 human-trace-to-workflow-graph 案例。若你允许非 LLM 时代的人类软件轨迹工作作为方法学前身，它完全可纳入 P2。 |
| [Lee23] | MobileGPT: Augmenting LLM with Human-like App Memory for Mobile Task Automation | 该工作从 mobile app 中的具体交互经验构建 memory，来源包括 random explorer 的自动探索轨迹与 user trace monitor 记录的人类操作轨迹。方法上，系统通过 Explore-Select-Derive 流程分析 screen interface，识别可执行 sub-tasks，并把已完成任务转写成 task、sub-task 与 primitive actions 的层级表示；同时将页面与子任务关系组织为 transition graph，并对功能相近的 screen 做节点复用。产物是三层 hierarchical memory，其中 task 由一组 node-subtask 对组成，node 对应具有相同功能集合的 app page，edge 对应可复用的 sub-task，sub-task 本身又展开为 action sequence，并以 function-call format 保存名称、描述与参数。下游时，系统在收到新指令后先做 recall，将当前任务与已有 task memory 对齐，若匹配成功则直接按 memory 中的 sub-task 与 action structure 执行，而不再重新进行完整推理。论文还描述了 attribute-based action adaptation、in-context action adaptation、HITL repair 和基于相似度的节点合并，用于处理参数变化、界面变化与记忆冗余。 | graph | mixed | 系统将当前任务匹配到 memory graph 中的 task path 后，直接按其中存储的 sub-task 和 action 序列执行。 | GUI / mobile | 归为 graph。核心 artifact 不是单个 callable skill，也不是整张按任务阶段调度的 workflow template，而是以 app page 为节点、以 sub-task 为边的 transition graph；但边内部确实承载过程步骤，所以与 workflow 子类有交叉。按你的 P2 判据，这篇应纳入，因为输入是具体交互轨迹，输出是显式关系与过程骨架，且下游执行依赖该结构而非仅依赖自由文本语义。 |
| [Tan25] | Agent KB: Leveraging Cross-Domain Experience for Agentic Problem Solving | 该工作从多种 agent framework 的 heterogeneous execution trajectories 与少量 human-curated seeds 中构建经验库，输入既包括成功与失败运行得到的 execution logs，也包括由这些运行整理出的 workflow summaries 和 execution snippets。方法上，系统通过 human-guided abstraction 把不同框架的轨迹标准化，抽取 task embedding、goal constraints、action-reasoning pairs 与 cross-framework metadata，并将其组织为统一的 experience unit；在应用阶段再通过 Reason-Retrieve-Refine 流程，把检索到的 past experiences 综合成 preliminary execution plan 与 targeted fixes。产物的核心单位定义为 \(E=\langle \pi,\gamma,S,C\rangle\)，其中 \(\gamma\) 是 structured predicates，\(S=\{(a_i,r_i)\}\) 存储 action-reasoning pairs，附录示例还显示它会保存 agent planning、search agent planning 以及 generalized experience principles。下游时，系统先用 BM25 加 embedding ranking 检索相关 experiences，再通过 disagreement gate 过滤冲突项，并把选中的 cross-domain workflows 与 diagnostic fixes 注入当前 agent 的 planning and execution loop。论文还描述了 self-evolving memory，包括新条目加入、LLM ranker 去重比较，以及基于 recency、frequency 和 transfer success 的 adaptive eviction。 | workflow | mixed | 检索到的 experience units 被综合成 preliminary execution plan 和 candidate fixes，注入 agent 的规划与执行过程。 | general | 这是一个边界案例。就 artifact 本体看，它不是纯代码 skill，也不是显式 graph；最接近的是 workflow 类，因为论文明确写到用 retrieved experiences 生成 “coherent workflow” 和 “preliminary execution plan”，附录示例中的 `agent_planning` 也是编号步骤式过程结构。但同一条目里还混有 `agent_experience` 这类原则性文本，所以它不是特别“干净”的纯 schematic artifact。若你的小节允许“workflow summary + structured planning fields”的混合表示，可纳入；若你只收显式 SOP、DSL、DAG、task tree 这类更强形式化 artifact，则可把它标为弱 P2 或边界文献。 |
