
| 引用标识 | 标题 | 关键思想与方法 | 记忆库类型 | 轨迹聚合 | 产物形态 | 领域 | 备注 |
|---|---|---|---|---|---|---|---|
| [Shi23b] | Reflexion: language agents with verbal reinforcement learning | 该方法把单次 trial 的交互轨迹与环境反馈一起作为输入，经验来源是 agent 自己在环境中的试错执行。Self-Reflection model 在每轮结束后对失败轨迹做 post-hoc reflection，生成针对错误原因与改进方向的 verbal feedback。产物是短文本 reflection 或 lesson，作为一段自然语言经验被写入当前任务的 memory buffer。该 buffer 在后续尝试中直接拼接进 actor prompt，旧条目本身不改写，只在上下文超长时截断保留最近若干条。 | 静态 | 单条 | 短文本 reflection 列表，经追加到 prompt 的方式在后续 trial 中直接消费。 | general | 跨 reasoning、code、interactive environment 多域实验，主实验不止一个。它更像 task-local 反思缓存，持久库形态较弱。 |
| [Aza25] | Reflection-Based Memory For Web navigation Agents | 该方法使用 base web agent 在 WebArena 上跑出的成功与失败历史轨迹，经验来源是 agent self-generated trajectories。系统对每条历史轨迹调用 LLM 做 reflection，可选 one-shot、summary 或 Web-Reflection 模式，将计划、结果与失败原因压缩成经验文本。产物是与任务 embedding 绑定的 reflection 条目，存入 key-value knowledge base。测试时按当前任务检索 top-k 相关 reflections 并注入 web agent prompt。 | 静态 | 单条 | 单条 web trajectory 对应一条 reflection，检索 top-k 后作为 retrieval-augmented prompt context 使用。 | web | 库中会同时取回多条 reflection，但单个条目仍是由单条轨迹生成。 |
| [Liu23] | REFLECT: Summarizing Robot Experiences for Failure Explanation and Correction | 该方法处理的是机器人一次执行中的多模态经验，包括 RGB-D 视频、音频、状态与执行记录，来源是 robot self-execution。系统先把原始观测转成 scene graph、事件级 caption 与 subgoal-level summary，再做 hierarchical summarization。产物是分层文本摘要，描述关键动作、环境变化与失败相关证据。该摘要随后被送入 LLM 用于 failure explanation 与 correction planning，而不是维护跨 episode 的持久经验库。 | 静态 | 单条 | 分层机器人经验摘要，经送入 LLM 生成 failure explanation 和 correction plan。 | embodied / robotics | 论文内部会更新 scene graph 与感知表示，但那是单次总结流程的一部分，不是跨任务经验库的持续演化。 |
| [Hua25] | MemOrb: A Plug-and-Play Verbal-Reinforcement Memory Layer for E-Commerce Customer Service | 该方法把客服多轮对话轨迹及其结果作为经验输入，来源是在线 customer service interactions。系统在每次会话后把新轨迹压缩成一个 Orb，用 LLM 提炼出带情绪、上下文和 outcome 的 policy reflection。产物是六元组形式的 verbal-reinforcement memory unit，其中核心内容是面向后续服务策略的文本反思。后续轮次先重写当前查询以便检索，再把相关 Orbs 取回注入 prompt。 | 静态 | 单条 | Orb 六元组，含 obs、emotion、outcome、context 等字段，经检索后拼入客服 agent prompt。 | customer service | 论文里的 rewrite 主要是查询重写，不是对旧 Orb 条目的改写。 |
| [Hu25] | Sample-Efficient Online Learning in LM Agents via Hindsight Trajectory Rewriting | 该方法从 agent 在线执行得到的单条失败轨迹出发，经验来源是 self-generated rollout。系统做 hindsight trajectory rewriting，从失败执行中识别可达替代目标，并为这些目标重写出优化后的成功 workflow。产物是自然语言形式的 synthetic positive trajectories 或 workflow examples，被写入 replay buffer 作为后续 in-context experience。该 buffer 会在同一目标出现更短更优版本时替换旧条目，后续决策直接读取这些重写后的正例。 | 动态 | 不适用 | 重写后的自然语言成功 workflow 或 positive example，存入 replay buffer 并作为后续 prompt 中的经验示例。 | general | 经验产物更接近 goal-relabeled rewritten trajectories，而不是简短 reflection 或 rule。 |
| [Sar25] | Self-Generated In-Context Examples Improve LLM Agents for Sequential Decision-Making Tasks | 该方法把 agent 成功完成任务后的完整轨迹加入数据库，经验来源是 agent self-generated successful trajectories，少量初始 exemplars 来自人工提供。系统不做强压缩，而是把成功 trajectory 以 in-context example 的形式保留，并在状态级或轨迹级做相似检索。产物是带 goal、plan、observation、reasoning 和 action 的成功示例轨迹。后续任务中，agent 检索多条相关示例并把它们作为 planning 或 action generation 的上下文。 | 静态 | 单条 | 成功 trajectory exemplars，经 kNN 检索后以 full-trajectory 或 state-window 方式注入 prompt。 | general | 该方法保留了较多原始轨迹细节，抽象压缩程度弱于典型 reflection 或 lesson 类方法。 |
| [Wan26ap] | MemGovern: Enhancing Code Agents through Learning from Governed Human Experiences | 该方法使用 GitHub 中 issue、PR、patch 的闭环修复记录，经验来源是 governed human experiences 而非 agent 自主试错。系统先做仓库与实例筛选，再用 LLM 将原始讨论与补丁过程标准化为 Experience Card，并通过 checklist 式 refine loop 保证字段质量。产物是分成 Index layer 与 Resolution layer 的字段化卡片，包含 problem summary、signals、root cause、fix strategy、patch digest 与 verification。代码 agent 在新问题上先搜索卡片索引层，再浏览 resolution 层做 analogical transfer。 | 静态 | 单条 | Experience Card 字段化文本卡片，经 progressive search 和 selective browsing 被代码 agent 消费。 | code | 经验来源是人类修复记录，不是 agent 自身 trajectory，这点和本节多数工作不同。 |
| [Zha23c] | ExpeL: LLM Agents Are Experiential Learners | 该方法把成功轨迹池以及失败到成功的对比经验作为输入，经验来源主要是 agent self-generated experiences。系统用专门的 insight model 从这些 experiences 中抽取可迁移的 natural-language insights，并对已有 insight 执行 ADD、EDIT、UPVOTE、DOWNVOTE。产物是持续演化的 insight memory，每条 insight 带有重要度并可随着后续经验被修改或移除。下游 agent 在新任务中检索相关 insights，将其作为决策提示注入 prompt。 | 动态 | 不适用 | insight bank，自然语言规则或经验洞见，检索后作为后续 agent prompt 的 guidance。 | general | 这是少数明确把 edit 和 vote 机制写成 memory operation 的工作。 |
| [Fu24] | AutoGuide: Automated Generation and Selection of Context-Aware Guidelines for Large Language Model Agents | 该方法从同一任务上的优劣轨迹对中抽取经验，来源是离线 collected successful 和 failed agent trajectories。系统先定位两条轨迹的 deviation point，再总结共享上下文，并围绕分叉动作抽出 context-aware guideline。产物是条件化自然语言规则，通常写成 when-context then-action 一类的 guideline 条目，并按 context 建成字典。测试时 agent 先识别当前 context，再检索并选择最相关 guidelines 注入 action prompt。 | 静态 | 多条 | 条件化 guideline 字典，经 context matching 和 LLM selection 后注入 prompt。 | web | 单个 guideline 依赖成功与失败轨迹的对照构造，属于典型的 pairwise contrastive extraction。 |
| [Ni26] | Trace2Skill: Distill Trajectory-Local Lessons into Transferable Agent Skills | 该方法从多条成功与失败执行中收集 trajectory-local lessons，经验来源是 agent rollouts。系统先由并行 analyst 对各条轨迹提出局部 patch，再通过分层 merge 与 conflict-free consolidation 把这些 patch 合成为可迁移 skill 更新。产物是以 SKILL.md 为核心的 skill directory，正文仍是自然语言 procedural knowledge，并可带辅助脚本或资源。已有技能文档可在 deepening mode 下被进一步改写和整合，后续 agent 直接读取这些技能文档执行任务。 | 动态 | 不适用 | 以 SKILL.md 为核心的技能文档，可附带辅助资源，作为外部 procedural context 直接供 agent 使用。 | general | 产物仍有明显自然语言主体，但外围带资源文件，形态比普通 lesson bank 更厚。 |
| [Not24] | Skill Set Optimization: Reinforcing Language Model Behavior via Transferable Skills | 该方法从 actor 历史中抽取多个相似 subtrajectory，对经验来源于 agent self-generated interactions。系统对候选子轨迹做 similarity、future reward 与 length 打分，再选出高价值组合，由 generative LLM 总结成抽象 subgoal 与 instruction list。产物是带 initial state、subgoal 与 numbered instructions 的 transferable skill。skill set 在后续迭代中依据实际 observed value 被持续筛除和替换，agent 通过检索当前状态相近的 skill 来指导行为。 | 动态 | 不适用 | 自然语言 skill 单元，含 subgoal 与步骤说明，经 state-based retrieval 后供 agent 执行。 | general | 技能项带明显字段结构，但消费方式仍是自然语言指导。 |
| [Che25d] | SWE-Exp: Experience-Driven Software Issue Resolution | 该方法把代码修复 agent 的完整 repair trajectories 作为输入，经验来源是 agent 在软件仓库中的解决过程。系统从成功和失败尝试中抽取 comprehension experiences 与 modification experiences，将问题理解模式和修复策略分别泛化成经验条目。产物是带 perspective、modification、issue type 与 description 的 experience bank 条目。新 issue 到来时，系统检索并重排相关经验，再由 instructor 和 assistant 两个 agent 把经验改写成更稳健的高层指导与执行建议。 | 静态 | 单条 | 结构化 experience bank 条目，经向量检索、reranking 与再表述后供 code agent 使用。 | code | 论文强调 bank 的持续扩充，但未明确写出对旧条目的编辑或合并机制。 |
| [Su25] | Mistake Notebook Learning: Batch-Clustered Failures for Training-Free Agent Adaptation | 该方法以一批失败样本及其错误轨迹为输入，经验来源是模型在批量任务上的失败表现。系统先按高层 subject 对 failures 做语义聚类，再让 Tuner Model 为每个 subject 生成包含 corrected examples、correct approach、mistake summary、generalizable strategy 与 anti-patterns 的 mistake note。产物是按 subject 组织的 notebook entries，并带有 embedding 用于检索。每轮更新都要经过整批 post-update evaluation，只有 notebook 新版本整体提升时才接受，因此后续推理使用的是被保留的 notebook state。 | 动态 | 不适用 | subject-indexed mistake notes，经 embedding retrieval 与 applicability assessment 后注入 prompt。 | reasoning | 更新单元是批量失败簇，不是在线逐条写入；其接受或回滚机制比常见 append-only notebook 更严格。 |
| [Che24] | AutoManual: Constructing Instruction Manuals by LLM Agents via Interactive Environmental Learning | 该方法让 Planner 在环境中交互并生成代码执行轨迹，经验来源是 agent self-generated interactive learning trajectories。Builder 根据成功、间接成功和失败等 case-conditioned prompts 从轨迹中抽取规则，Consolidator 在规则过多时合并冗余并删除过时项，Formulator 再把规则整理成 Markdown manual。产物是带分层章节、规则类型、适用范围、示例与 validation logs 的 instruction manual。测试时手册直接作为外部文档供 agent 读取，指导后续长程执行。 | 动态 | 不适用 | Markdown instruction manual，经整体文档注入或读取的方式指导后续 agent。 | embodied / robotics | 主实验是 ALFWorld，虽也可视作文本环境，但论文叙事明显围绕 embodied-style interaction。 |
| [Wu26] | Procedural Knowledge at Scale Improves Reasoning | 该方法使用 teacher model 生成的 reasoning trajectories，经验来源是大规模 teacher-synthesized traces 而非 agent 自主环境交互。系统把长推理链切分成 subquestion-subroutine pairs，把局部求解过程压缩成更高层的程序性自然语言步骤。产物是静态 procedural datastore，其中每个条目由子问题及其可复用 subroutine 组成。推理时模型先在思维过程中 verbalize 当前子问题，再检索相关 subroutine 作为 hint 注入 reasoning stream。 | 静态 | 单条 | subquestion-subroutine 对，经 in-thought retrieval 后以 hint 形式插入推理上下文。 | reasoning | 经验源与实验形态更接近 reasoning traces 而非典型 agent-environment trajectory，可作为本节外延材料看待。 |
| [Suz25] | Dynamic Cheatsheet: Test-Time Learning with Adaptive Memory | 该方法把模型在连续任务上的解答与其中可复用部分作为经验输入，来源是 test-time sequential interactions。Curator 对新答案做 usefulness 与 generalizability 评估，把可迁移内容蒸馏成 cheatsheet 中的策略、solution sketch 或 code snippet。产物是持续维护的 cheatsheet 文本，旧条目可以被改写、删去、合并或压缩，以控制长度并提升可读性。后续 query 会连同当前 cheatsheet 一起送入 generator，或先经 retrieval-assisted refinement 再解题。 | 动态 | 不适用 | cheatsheet 文本，经整体注入或检索增强后作为后续问题的外部记忆。 | reasoning | 这类 memory 以整份文档为更新对象，更新粒度比常见条目式 memory 更粗。 |
| [Maj23] | CLIN: A Continually Learning Language Agent for Rapid Task Adaptation and Generalization | 该方法把每次 trial 的交互历史作为输入，经验来源是 agent self-generated interactions。memory generator 从成功与失败轨迹中抽取因果式语言陈述，如某动作对某结果是 necessary 或 does not contribute，并根据不确定度调整措辞。产物是 causal abstraction memories 与由优质 memories 再总结得到的 meta-memory。系统在每次 trial 后都会基于当前轨迹与最近记忆生成更新版本，并在后续目标选择时检索相关 learnings 注入 controller prompt。 | 动态 | 不适用 | 因果式自然语言 memory 与 meta-memory，经检索后用于 goal selection 和 planning。 | general | 其核心 abstraction 是因果陈述而非普通经验总结，这使得条目颗粒度较细。 |
| [Hua25e] | R2D2: Remembering, Replaying and Dynamic Decision Making with a Reflective Agentic Memory | 该方法同时利用历史 web trajectory 与 query，经验来源是 web agent 过往任务执行。系统把相关轨迹截断、纠错并附加 reflective insights，形成 query-keyed reflective memory；同时把导航轨迹写入 replay graph 供动态决策。产物包括 corrected reflective trajectory entries 和用于路径搜索的 replay buffer。若新轨迹能为同类 query 提供更准确或更丰富的反思，系统会更新旧值，测试时再把检索到的反思与 replay path 一起用于决策。 | 动态 | 不适用 | query-keyed reflective trajectories 与 replay graph，经 retrieval 和 path search 共同指导 web action。 | web | 该框架同时含 reflective memory 与可搜索 replay graph，两者共同工作。 |
| [Mi26b] | Darwinian Memory: A Training-Free Self-Regulating Memory System for GUI Agent Evolution | 该方法以 GUI agent 的子任务计划与执行轨迹为经验输入，来源是 agent 在 GUI 环境中的持续探索。系统把每段经验写成以 precondition 和 goal 为索引的 memory unit，内含对应执行轨迹与元数据，并通过 evolutionary replacement、survival-based pruning 与 reputation-style inhibition 调节记忆库。产物不是简短 lesson，而是 plan-indexed reusable trajectory memory。后续执行时 agent 用 dual-factor retrieval 命中对应 memory，直接复用其轨迹，必要时再通过 epsilon-mutation 探索更优版本。 | 动态 | 不适用 | plan-indexed memory tuple，含自然语言 precondition 和 goal 及执行轨迹，经检索后直接 replay。 | GUI / mobile | 存储内容保留了较完整的执行序列，叙述性抽象程度相对有限。 |
| [Yan26b] | M2: Dual-Memory Augmentation for Long-Horizon Web Agents via Trajectory Summarization and Insight Retrieval | 该方法把当前会话轨迹与历史成功日志同时作为经验来源，前者来自在线 self-generated interaction，后者来自离线 web tasks。系统一方面在每步把当前 observation、thought 和 action 压成简短 textual state summary，形成递归 internal memory；另一方面用 strategy abstractor 从历史成功日志中提炼 topic-tagged insights，构成 external insight bank。产物包括当前任务的 summary chain 和离线 insight entries，两者都以自然语言形式存在。测试时 internal memory 作为紧凑历史上下文持续拼接，external insights 通过语义检索后作为 Defensive Hints 注入 system prompt。 | 静态 | 单条+多条 | 递归文本状态摘要链加离线 insight bank，分别作为紧凑历史与检索到的 hints 注入 prompt。 | web | 双记忆层的构造来源不同，internal memory 明确是单轨迹压缩，external insight bank 的抽象更接近跨日志归纳。 |
| [Yan25d] | Learning on the Job: An Experience-Driven Self-Evolving Agent for Long-Horizon Tasks | 该方法把长程任务执行中的 dilemma、成功子任务轨迹与工具使用历史作为经验输入，来源是 agent self-generated long-horizon interactions。系统分别把这些经验转成 strategic memory 中的 dilemma-strategy 对、procedural memory 中的 SOP，以及 tool memory 中的动态操作提示，并在任务后做统一 refinement、integration 与 deduplication。产物是三类自然语言或半结构化 procedural artifact，共同构成分层 memory module。后续任务中，strategic memory 直接装入系统提示，procedural memory 按需检索完整 SOP，tool memory 在工具调用时返回即时指导。 | 动态 | 不适用 | 分层 memory，包括 dilemma-strategy、SOP 与 tool instruction，经全局注入和按需检索共同消费。 | general | 方法是典型多记忆库设计，轴一判定主要由其显式 refine、merge、dedup 决定。 |
| [Cao25] | Remember Me, Refine Me: A Dynamic Procedural Memory Framework for Experience-Driven Agent Evolution | 该方法从成功轨迹与初次失败后自我反思成功的经验中抽取知识，来源是 agent self-generated tasks。系统把经验写成带 usage scenario、core experience、keywords、confidence 与 tools 的 structured experience entry，并在 acquisition 阶段做验证与去重。产物是结构化 procedural memory pool，检索时还会进行 context-adaptive rewriting，把取回经验改写成当前任务可用的指导。库本身会基于 utility 做 pruning，并持续添加新经验，但存储态条目未描述为 in-place rewriting。 | 动态 | 不适用 | 结构化 experience entries，经检索与 inference-time rewriting 后注入 prompt。 | general | 其动态性主要来自删除与筛选，而不是对单个旧条目的正文重写。 |
| [Liu25] | Contextual Experience Replay for Self-Improvement of Language Agents | 该方法把 web agent 的历史轨迹作为输入，经验来源是 online self-generated web interactions。系统不保留原始长轨迹，而是把每次经验蒸馏成 environment dynamics 与 skills 两类自然语言摘要，并在加入 buffer 时参考已有内容避免重复。产物是动态 memory buffer，既有页面和 URL 的可用性总结，也有带占位符的 step-by-step skill。新任务到来时，系统检索最相关的 dynamics 和 skills，把它们转成自然语言块加入当前上下文实现 experience replay。 | 动态 | 不适用 | dynamics 与 skill summaries，经检索后作为 replayed natural-language context 注入 web agent prompt。 | web | 该方法明确把 replay 对象从 raw trajectory 转成摘要化经验，这是其与 exemplar retrieval 的关键区别。 |
| [Yan26] | AutoSkill: Experience-Driven Lifelong Learning via Skill Self-Evolution | 该方法从用户交互信号中抽取可复用能力模式，经验来源主要是 user-side interactions 而非 agent 执行轨迹本身。系统把候选能力表示成 skill，并通过 prompt-driven judge 决定 add、merge 或 discard，再对保留下来的技能执行 versioned evolution。产物是以 SKILL.md 为主体的 skill artifact，含名称、描述、prompt body、trigger、tags、examples 与 version。后续响应生成时先检索相关 skills，再把选中的 skills 渲染成 compact external memory context 注入 prompt。 | 动态 | 不适用 | versioned skill artifacts，经 hybrid retrieval 后作为外部 skill context 注入生成 prompt。 | general | 经验源偏向用户需求信号，和“从 agent 自身交互轨迹抽象经验”相比略有不同。 |
| [Xie25] | GUI-explorer: Autonomous Exploration and Mining of Transition-aware Knowledge for GUI Agent | 该方法从 GUI 自主探索得到的 observation-action-outcome triples 中抽取经验，来源是 GUI agent self-exploration。系统为每次状态转移抽出 visual key 与对应的 textual operational value，并在新值与旧值足够不同但 key 匹配时把 value 合并到旧条目。产物是存于 multimodal knowledge store 中的 transition-aware knowledge entries，文本部分描述某 UI 元素触发的操作效果与约束。任务执行时，系统对当前界面元素做检索与排序，再把相关知识整理成 dynamic guidance 注入 agent prompt。 | 动态 | 不适用 | 视觉键加文本操作知识的多模态条目，经检索排序后生成 GUI guidance。 | GUI / mobile | 产物含视觉 patch 键，消费时也依赖视觉语义匹配，不是纯文本 memory。 |
| [Ma26b] | SkillClaw: Let Skills Evolve Collectively with Agentic Evolver | 该方法汇总多个用户与 agent 的真实使用 session，经验来源是 distributed agents in the wild 的交互轨迹。Agentic Evolver 对聚合后的成功与失败证据做开放式分析，判断是 create 新 skill、refine 旧 skill 还是 skip，并在夜间验证后把有效更新同步回共享仓库。产物是以 SKILL.md 表达的可复用技能文件，通常含触发条件、禁用条件、类别和详细操作指导。白天推理时，各 agent 从共享 SkillHub 中选择相关 skills 加载进上下文，形成群体层面的经验复用。 | 动态 | 不适用 | 共享 SKILL.md 技能仓，经同步部署与按需加载供在线 agent 使用。 | general | 经验聚合范围是多用户多 agent，会比单 agent 自演化方法更强调 collective refinement。 |
| [Yan25] | Coarse-to-Fine Grounded Memory for LLM Agent Planning | 该方法使用环境基础信息、人工示范与离线 trial-and-error 轨迹，经验来源混合了 manual trajectories 与 agent collected experiences。系统先得到 coarse focus points，再比较成功与失败轨迹提炼 hybrid-grained tips，在线阶段则从当前轨迹中抽出 fine-grained key information 做异常修正。产物包括 focus points、tips dictionary 与 online key information，其中真正可复用的长期库主要是 tips dictionary。测试时 agent 检索相似成功案例与对应 tips，并在异常时结合 key information 做 self-QA reflection 与 corrective plan 生成。 | 静态 | 多条 | 分层 memory 中的长期部分主要是 tips dictionary，经相似案例检索与在线 corrective reflection 共同消费。 | general | 其长期库更接近成功失败对照提炼的 tips；fine-grained key information 更像当前轨迹内的临时分析。 |
| [Ye25b] | H2R: Hierarchical Hindsight Reflection for Multi-Task LLM Agents | 该方法把多任务执行轨迹作为经验输入，来源是 agent self-generated multitask interactions。系统在高层从完整任务轨迹反推出已实现的 subgoal sequence 与 planning insights，在低层把成功子轨迹按 subgoal 分段并提炼 execution insights。产物是高层与低层两套 hindsight memories，分别附着 task description、subgoal sequence 或 execution trajectory。memory pool 支持 add、modify、upvote、downvote，后续 planner 检索高层记忆，executor 检索低层记忆并分别注入各自 prompt。 | 动态 | 不适用 | 高层 planning memory 与低层 execution memory，经分层检索后分别供 planner 和 executor 使用。 | general | 该方法把 planning insight 与 execution insight 明确拆层，是较少见的双层 reflection 设计。 |
| [Fan25] | Memp: Exploring Agent Procedural Memory | 该方法把历史任务经验转成 verbatim trajectories、scripts 或两者结合的 proceduralization，经验来源是 agent past interactions。系统先按任务相似度检索最相关 procedural memory，再在后续成功或失败后执行 add、adjust update 或 delete。产物既可以是具体示例，也可以是较高层的 script-like procedural memory，并可在 update 时把新旧信息合成 revised memory。后续 agent 根据 query 或关键词检索 memory item，将其作为 procedural guidance 注入推理过程。 | 动态 | 不适用 | trajectory、script 或 hybrid proceduralization，经相似检索后作为外部 procedural context 使用。 | general | 同时容纳 verbatim 与 abstract 两种形态，使其在本节内部也偏混合型。 |
| [Ma25] | What Deserves Memory: Adaptive Memory Distillation for LLM Agents | 该方法把原始 episode 与由现有知识预测出的 anticipatory schema 对比，经验来源是 agent sequential episodes。系统用 prediction error 识别值得保留的信息，并把其蒸馏成 semantic knowledge，同时把有连续性的 episodes 融合成 narrative episodic memories。产物是双层 memory，包括 narrative episode database 与 semantic knowledge database。两层库都会执行 merge、conflict replacement 或新建插入，后续回答时并行检索 episodic 与 semantic memories 拼接进生成上下文。 | 动态 | 不适用 | narrative episodic memories 加 semantic knowledge entries，经并行检索后共同供生成模块使用。 | general | 方法重心在“什么值得记忆”的蒸馏准则，实验场景比典型 agent benchmark 更广义。 |
| [Zha25f] | Agentic Context Engineering: Evolving Contexts for Self-Improving Language Models | 该方法把模型成功与错误 reasoning trajectories 作为经验输入，来源是离线或在线的 model-generated traces。Reflector 从轨迹中抽出 concrete insights、failure modes、strategy bullets 或 code snippets，Curator 再把它们作为 delta context 合并进 Context Playbook。产物是由细粒度 bullet 组成的 playbook，每条 bullet 带 id 与 helpful 或 harmful 计数。后续任务中 generator 直接使用 playbook 作为 system context，或只检索最相关 bullets 进行更细粒度注入。 | 动态 | 不适用 | 带元数据的 bullet-style context playbook，经整体注入或细粒度检索后消费。 | general | 该工作讨论的是 context artifact 演化，和传统 memory bank 的边界比普通 reflection 方法更宽。 |
| [Wan26] | SkillX: Automatically Constructing Skill Knowledge Bases for Agents | 该方法从强 agent 在训练任务上的成功 rollouts 中抽取经验，并用后续探索继续扩展覆盖面。系统先抽 planning skill，再引导 functional skill 与 atomic skill 的提取，并通过 merge、filter 与 exploratory expansion 持续优化 SkillKB。产物是三层 skill library，分别对应高层计划、宏观功能调用和原子工具用法说明。新任务到来时，系统先检索 planning skill 重写成 pseudo-plan，再据此分层检索 functional 与 atomic skills，最后把选中技能一次性注入 prompt。 | 动态 | 不适用 | 三层 SkillKB，经 pseudo-plan 驱动的分层检索后作为外部技能上下文注入。 | general | 虽然 planning skill 可写成自然语言步骤，但 functional 与 atomic 层的结构化程度更强。 |
| [Liu25e] | WebCoach: Self-Evolving Web Agents with Cross-Session Memory Guidance | 该方法把跨 session 的完整 web episodes 作为经验来源，数据来自 web agent 已完成的成功或失败任务。WebCondenser 把每段完成的 episode 压缩成 3 到 5 句 summary，并保留 success status 与 fail modes 或 success workflows 之类的结构化字段。产物是存入 External Memory Store 的 condensed episodic summaries。后续任务中 Coach 先检索最相关 memories，再决定是否介入，并把 1 到 2 句 advice 作为 system message 注入正在执行的 web agent。 | 静态 | 单条 | 完成 episode 的 condensed summary entry，经 top-k 检索与 coach 生成 advice 后插入系统提示。 | web | 论文强调只存完成 episode，不存半成品过程，这是一种库前过滤而非库内改写。 |
| [Fen25] | Get Experience from Practice: LLM Agents with Record & Replay | 该方法把多轮实践中得到的 interaction traces 记录下来，经验来源是 agent 在任务中的持续执行。系统把经验总结为高层 procedural experiences、低层具体执行 experiences 与配套 check functions，并在 repository 中做 updating、merging 与 ranking。产物兼有高层抽象和低层可执行表示，部分低层项可直接转成 API 调用或脚本。后续 agent 通过 record-and-replay 选择合适经验并在执行时回放或复用。 | 动态 | 不适用 | 高层经验、低层记录与 check functions 共同组成 repository，经 ranking 后用于 replay。 | general | 低层经验明显带可执行性，和纯 narrative artifact 有一定距离。 |
| [Xie25b] | Mirage-1: Augmenting and Updating GUI Agent with Hierarchical Multimodal Skills | 该方法从 GUI agent 的成功探索轨迹中抽取经验，来源是 online GUI exploration trajectories。系统把单条历史轨迹概括成 execution skill，再由多条 execution skills 上卷成 core skill 和 meta skill，并在新轨迹到来时通过 RefineSkillModule 继续增补和合并。产物是 hierarchical multimodal skills，底层保留 screenshot、step-goal 与 action sequence，上层则是更抽象的功能和类别。后续 planner 检索 meta 和 core skills 做子目标分解，decision reflector 参考 execution skills 评估动作。 | 动态 | 不适用 | 分层多模态技能库，经 planner retrieval、reflector scoring 与 SA-MCTS 共同消费。 | GUI / mobile | execution skill 层保留较多原始视觉与行动细节，叙述性抽象与多模态记忆并存。 |
| [Wu25] | Meta-Policy Reflexion: Reusable Reflective Memory and Rule Admissibility for Resource-Efficient LLM Agent | 该方法从失败 episode 中提取经验，来源是 agent self-generated failed trajectories。系统对失败点做 retrospective reflection，把纠正规则写成带权重的 predicate-like reflective memory，并在训练回合中持续把新规则并入 memory。产物是外部化的 meta-policy memory rules，而不是长文本反思段落。测试阶段 memory 冻结，agent 先检索相关 rules 注入 prompt，再通过 hard admissibility checks 过滤不合法动作。 | 静态 | 单条 | 带权重的 reflective rules，经检索注入和 post-generation admissibility check 共同消费。 | general | 规则表达比普通 natural-language reflection 更形式化，接近轻量 rule set。 |
| [Liu25b] | Learning from Online Videos at Inference Time for Computer-Use Agents | 该方法在测试时从在线视频教程中获取经验，经验来源是 external human video demonstrations 而非 agent 历史。系统先检索相关视频，再把视频处理成带 textual objective、action sequence 和 observation sequence 的 structured demonstration trajectories。产物是针对当前任务临时构造的 demonstration pool，没有论文中预先维护的全局持久库。执行过程中，系统根据当前状态从候选 demonstrations 中选择最相关片段，并把该轨迹段作为 in-context guidance 注入 computer-use agent prompt。 | 静态 | 单条 | 结构化 demonstration trajectories，经在线筛选后作为 task-specific in-context guidance 使用。 | GUI / mobile | 经验源来自外部视频，且库是 task-time on-the-fly 构建，这两点都和常见自我记忆方法不同。 |