

| 引用标识 | 标题 | 关键思想与方法 | Composite 归属判定 | 子类判定 | composition topology | constituent pathways | 领域 | 备注 |
|---|---|---|---|---|---|---|---|---|
| [Xia25e] | UI-Genie: A Self-Improving Approach for Iteratively Boosting MLLM-based Mobile GUI Agents | 论文以 AndroidControl、AMEX、AndroidLab 的人类 GUI 轨迹为起点，先把原始 screenshot-action history 转成统一的 reward-model 训练样本，样本既包含 rule-based verified positives，也包含 trajectory corruption、hard negatives 与探索后续 continuation 带来的潜在正例。随后训练 UI-Genie-RM，将当前 GUI 轨迹编码为图文序列并输出 step-level 与 task-level 二值 reward，再用该 RM 对 UI-Genie-Agent 的多候选 rollout 做 reward-guided beam search。对失败轨迹，方法额外通过 sampled continuations 做 potential-based labeling，把可能通向成功的中间 step 重新标成可学习监督。接着分别用新增 RM 标注数据更新 reward model，用 reward-validated synthetic trajectories 对 agent 做 SFT，并重复多轮。质量控制覆盖 action-level rule check、task completion verification、hard negative mining 与 step-level potential labeling。 | 属于 Composite | §7.1 | 闭环式 | `P4→P7→P5↺` | GUI / mobile | 若去掉 RM-guided exploration、continuation relabeling 与双数据回流，方法基本退化为常规合成轨迹加 SFT，核心贡献不再成立。RM 明确随 agent 新探索数据重训，且论文用 round-wise 提升和 unified reward ablation 证明 evaluator-policy 共演化是关键；同时有 rule verifier 与 hard negatives 抑制错误累积。 |
| [Li26n] | MagicGUI-RMS: A Multi-Agent Reward Model System for Self-Evolving GUI Agents via Automated Feedback Reflux | 论文以 AndroidControl 与 MagicGUI-Agent-39k 中的成功 GUI 执行轨迹和标注交互为经验源，先训练一个 domain-specific reward model，令其对 agent 提议动作输出 correctness、rationale 与 corrected action。其后再由一个更强但冻结的 general-purpose reward model 对原动作与纠正动作做二次语义评估、completion 判断与 preference 选择，从而确定最终用于执行和回流的动作。衔接机制是 automated feedback reflux：GP-RM 认可的高质量动作回流为 agent 监督数据，DS-RM 与 GP-RM 的分歧样本则回流为 DS-RM 的重点再训练数据。参数更新上，UI Agent 先 SFT 再 RLFT，DS-RM 先多输出监督训练再按 correctness reward 做 RLFT，GP-RM 保持冻结。质量控制包括规则校验、结构化负样本构造、intention-centric grounding correction 与 disagreement filtering。 | 属于 Composite | §7.1 | 闭环式 | `P4→P7→P5↺` | GUI / mobile | 把 reflux 换成普通收集后直接微调，方法最核心的双 RM 协同与 disagreement-driven 刷新就不成立。子类归到 §7.1，因为被学习的 DS-RM 被专门设计为随 agent 新数据持续刷新；Table 4 和 Table 5 也直接支持这一点。 |
| [Li26l] | No More Stale Feedback: Co-Evolving Critics for Open-World Agent Learning | 论文以当前 policy 在 WebShop、ALFWorld、SciWorld、DeepSearch 等环境中的 on-policy trajectory 为经验源，先让 actor 产生初始 rollout 及其基线得分。随后 critic 基于整条轨迹生成多条自然语言 diagnostic feedback，policy 再把这些 critique 拼接回输入，条件化地产生多条 refined trajectories。衔接机制是 cascaded hindsight diagnosis-and-refinement：critic 的反馈质量不以文本自身衡量，而以其诱导出的 trajectory gain 衡量。训练时 actor 与 critic 用 synchronized dual-track GRPO 同步更新，critic 奖励采用 saturation-aware gain，外加 KL 约束与 group-relative normalization 稳定共训。质量控制主要来自 SA reward shaping、同批归一化和无 evolving critic 的对照消融。 | 属于 Composite | §7.1 | 闭环式 | `P4→P7→P5↺` | general | 若把 evolving critic 换成训练后冻结的 critic，论文自己报告性能显著下降，说明 stale feedback 恰是核心问题。其衔接机制不是普通 actor-critic，而是 critic 随 policy failure mode 同步刷新并通过 token critique 回流；Table 2 是关键证据。 |
| [Wan26u] | RLAnything: Forge Environment, Policy, and Reward Model in Completely Dynamic RL System | 论文从当前 policy 在 OSWorld、AlfWorld、LiveBench 等环境中的 rollout 出发，先得到 trajectory-level verifiable outcome，再由 reward model 给出 step-wise evaluation。然后把 outcome reward 与 step reward 融合成 policy update 的训练信号，并反过来用 trajectory consistency 去更新 reward model。与此同时，reward model 总结出的 failure pattern 还会驱动 task difficulty adjustment，生成更易或更难的新任务供后续训练。参数层面同时更新 policy 与 reward model，环境侧通过 LLM 做 task rewriting 而非梯度训练。质量控制依赖 difficulty acceptance threshold、task-essence preservation 约束与多组件 ablation。 | 属于 Composite | 跨类 | 闭环式 | `P7→P4→P6↺` | general | 去掉 reward-policy-environment 的联动，只保留普通 RL，方法不再是 RLAnything。其一支是 reward model 随 policy 经验刷新，接近 §7.1；另一支是 failure-driven task synthesis，作用于经验集合分布，接近 §7.3，因此判跨类；同时环境改写属于 7 条 pathway 之外的新转化形式。 |
| [Zha25y] | RL Tango: Reinforcing Generator and Verifier Together for Language Reasoning | 论文以 Eurus-2 的数学问答数据为起点，先对 generator 做 SFT warm start，再让 generator 与 verifier 在 RL 中交替更新。每轮中 verifier 对 generator 的多步推理生成 step-wise 正误 judgment，并把这些 judgment 归一化成 process reward，与 outcome reward 混合作为 generator 的优势信号。反过来 verifier 自身只用 generator 最终答案的 outcome correctness 与 format reward 学习，从而持续适应 generator 演化出的新推理分布。衔接机制是 interleaved co-training，而非固定 verifier 打分后单向训练 policy。稳定化设计包括 verifier warm-up、step/outcome advantage 分离归一化、alpha 衰减和 class-aware reweighting。 | 属于 Composite | §7.1 | 闭环式 | `P4↔P5` | reasoning | 若换成固定 verifier，文中在算法推理任务上的对照显示会明显落后，说明 verifier refresh 是必要设计。尽管场景偏 reasoning，但 evaluator-policy 共演化非常明确，符合 §7.1 的硬判据。 |
| [Sin25b] | VARP: Reinforcement Learning from Vision-Language Model Feedback with Agent Regularized Preferences | 论文从机器人当前 policy 在 MetaWorld、DMControl 中采集的 trajectory segment 出发，把 3D 运动序列投影成叠加在末帧图像上的 2D sketch，再把成对 sketched observations 输入固定 VLM 生成自然语言分析与三值 preference label。接着用这些 preference 训练一个 Bradley-Terry reward model，并以该 reward model 重标 replay buffer 后用 SAC 更新 policy。衔接机制是 agent-regularized preferences：reward model 的损失不仅拟合 VLM preference，还用当前 policy 轨迹的 return 正则化，以减轻 reward 与 actor 能力脱节。VLM 冻结，reward model 与 policy 交替更新，新策略持续产生新的 preference pair；同时会丢弃 indifference pair。质量控制主要依赖 sketch 提供时序上下文、indifference filtering 和 bilevel update。 | 属于 Composite | §7.3 | 闭环式 | `P7→P4→P6↺` | embodied / robotics | 若去掉 sketch-based preference construction 与 agent regularization，只剩普通 preference RL，论文主张不成立。这里的关键机制作用在“生成和筛炼何种 preference 数据分布”上，而不是刷新一个必须同 policy 校准的 evaluator，因此更接近 §7.3 而非 §7.1。 |
| [Pan26] | EVPO: Explained Variance Policy Optimization for Adaptive Critic Utilization in LLM Post-Training | 论文从当前 policy 的 on-policy rollouts 出发，同时计算 return 与 critic value，并在每个 batch 上估计 explained variance。随后依据 EV 的正负，在 critic baseline 与 batch-mean baseline 之间切换，再用对应优势值更新 policy；critic 自身则继续以 MSE 方式训练。中间机制是 adaptive baseline selection，而不是把经验改写成新的 token supervision 或经某个中介再转入下游训练。训练对象始终是 policy 与 critic 两个参数模块，主要实验在 math、Sokoban、FrozenLake、WebShop。质量控制来自 EV 阈值分析与 critic maturation 曲线。 | 不属于 Composite | 不适用 | 闭环式 | `P6` | general | 把 EV gating 换成固定 PPO 或 GRPO，方法仍是 critic-assisted RL 优化器，核心贡献依旧成立；因此不通过“中间衔接替换检验”。文中消融证明的是 baseline selection 的优化价值，而非经验转化链的必要性，更应退回单步 P6。 |
| [Wan26aj] | Co-Evolution of Policy and Internal Reward for Language Agents | 论文以 language agent 在 ALFWorld、ScienceWorld、WebShop 中的在线交互为经验源，令同一 policy 在每步先生成 verbal self-guidance，再条件化地输出动作。随后方法把这些 self-guidance 映射成正、零、负三值内部奖励，并与环境奖励加权求和后用于 GRPO。衔接机制是 stage-wise trust schedule：早期只学会产生 guidance，不把它作为奖励；中期逐步放大其影响；后期再退火以避免偏离真实任务目标。整个训练中没有单独的 evaluator 参数，policy 自身既生产 guidance 又消费 guidance 诱导的奖励。质量控制主要依赖 trapezoid trust schedule 与 offline distilled guidance 的失败对照。 | 边界 | §7.1 | 闭环式 | `P7→P5↺` | general | 若用普通环境奖励替代 self-guidance，方法的主旨会消失，因此有实质中间机制；但它没有独立的 learned evaluator 参数，严格说不满足 §7.1 的“刷新 evaluator 参数”硬判据。故记为边界，并在 constituent pathways 中仅写最接近形式；其中包含 7 条 pathway 之外的 token-to-intrinsic-reward 映射。 |
| [Sun24b] | OS-Genesis: Automating GUI Agent Trajectory Construction via Reverse Task Synthesis | 论文从无人工探索得到的 GUI state-action-state triple 出发，先把每个局部状态变化转写为 low-level instruction，再进一步抽象成 high-level task instruction。随后用强模型在真实环境中执行这些合成任务，得到完整多步 GUI trajectory。衔接机制是 reverse task synthesis 与 trajectory reward modeling：任务不是预先给定，而是由已观察到的功能回推出来，再由 TRM 按 completion 与 coherence 给生成轨迹打 1 到 5 分并按分数采样进训练集。训练对象是下游 GUI policy，GPT-4o 作为合成器和评分器保持冻结。质量控制集中在 trajectory-level reward scoring 与 reward-based sampling。 | 属于 Composite | §7.3 | 链式 | `P1→P7→P5` | GUI / mobile | 若把 reverse synthesis 和 TRM filtering 换成普通任务列表加普通 SFT，方法核心即不存在。其关键机制作用在“从探索中构造任务并筛炼任务对应轨迹”的经验集合生成上，所以归 §7.3。 |
| [He24e] | OpenWebVoyager: Building Multimodal Web Agents via Iterative Real-World Exploration, Feedback and Optimization | 论文先用 GPT-4o expert trajectory 对 Idefics2-8B 做 imitation learning，再让当前 web agent 在 self-instruct 生成的新任务上进行真实网页探索。探索所得 trajectory 由 GPT-4o 作为 auto evaluator 做 trajectory-level rejection sampling，只保留成功轨迹，并与原始 IL 数据混合后继续 SFT。衔接机制是 exploration-feedback-optimization 三段式回路，其中 evaluator 不直接给 reward，而是决定哪些 self-generated trajectories 能转成下一轮监督。模型参数只更新 web policy，teacher 与 evaluator 冻结；为控制长上下文，只保留最近若干截图和当前页面 AT 树。质量控制主要依赖 unfinished trajectory filtering、成功重采样和 trajectory rejection。 | 属于 Composite | §7.3 | 闭环式 | `P7→P5↺` | web | 若去掉 self-exploration 后的 evaluator-based acceptance，只剩普通 imitation extension，论文主贡献会显著削弱。这里 evaluator 是冻结的，作用主要是构造可训练经验集合，而非随 policy 共演化，故归 §7.3。 |
| [Xu24] | AgentTrek: Agent Trajectory Synthesis via Guiding Replay with Web Tutorials | 论文从 RedPajama 中抓取网页教程文本，先经规则预筛、LLM 标注与 FastText 扩展筛选，再用 GPT-4o mini 把原始教程标准化成 platform、prerequisite、step-by-step instruction 等结构化模板。随后一个 VLM agent 在真实 BrowserGym 页面上按教程做 guided replay，记录 screenshot、AXTree、reasoning 与 action 形成训练轨迹。其核心衔接是 tutorial-guided replay：不是直接把教程当监督，而是让教程先约束真实交互再生成可训练 agent trajectory。最后再用 GPT-4o 做 trajectory-level 和 step-level verification，把合格轨迹用于 text-based 与 vision-based web agent 的训练。质量控制覆盖多级语料过滤、真实执行验证与 earliest failure analysis。 | 属于 Composite | §7.3 | 链式(含局部迭代) | `P1→P7→P5` | web | 若删去 guided replay，仅保留教程收集后直接微调，方法不再成立；教程到真实轨迹的衔接正是贡献所在。其机制作用于经验集合构造与验证，属于 §7.3；局部 replay 阶段有环境交互迭代，但整体仍是单向链。 |
| [Wan24ad] | Bootstrapping Language-Guided Navigation Learning with Self-Refining Data Flywheel | 论文以少量人类 instruction-trajectory pair 和大规模无标签导航轨迹为起点，先训练 instruction generator 为无标签轨迹补写指令，再训练 navigator 追随这些指令。随后 navigator 用 SPL 与 nDTW 过滤高质量合成数据，高质量子集用于刷新 generator，低质量样本会被 generator 重新 caption 后再训练下一轮 navigator。衔接机制是 self-refining data flywheel：navigator 不只是最终消费者，还是 generator 的数据评估器。参数上 generator 与 navigator 都按轮次重训。质量控制依赖 SPL 等硬阈值过滤与多轮 re-captioning。 | 属于 Composite | §7.3 | 闭环式 | `P7→P5↺` | embodied / robotics | 把 navigator-based filtering 换成普通合成数据直接训练，flywheel 的核心就消失。虽然“评估器”是导航器而非独立 reward model，但机制明显作用于生成与筛炼训练集分布，因此归 §7.3。 |
| [Xu25q] | MCTS-EP: Empowering Embodied Planning with Online Preference Optimization | 论文先用 expert trajectories 对初始 embodied policy 做 warm-up，然后让当前 policy 在环境中做 MCTS 搜索。搜索树一方面产出成功 trajectory 供 SFT，另一方面依据 backup 后的 action value 在分支节点上编译出 preference pairs，供 DPO 更新 policy。衔接机制是 search-tree-to-preference compilation：偏好标签不是外部标注，而是从树结构中的 long-horizon value ranking 导出。训练只更新 policy，本身不训练独立 evaluator；quality control 依赖 success filtering、self-critic expansion pruning 与 value-based pair construction。该流程可多轮迭代，每轮由新 policy 产生新搜索数据。 | 属于 Composite | §7.3 | 闭环式 | `P7→P5↺` | embodied / robotics | 若去掉从搜索树到 success set 与 preference set 的编译，只剩普通 expert SFT 或普通 RL，方法主张不复存在。中间机制作用在经验集合的生成与选择，而非改写单条轨迹内容，所以归 §7.3。 |
| [Xia25h] | Self-Improving Vision-Language-Action Models with Data Generation via Residual RL | 论文从 base VLA 的成功 rollout 填充 offline buffer 开始，先冻结通用 VLA backbone，仅训练一个 residual actor 和 Q-function，使其在 base policy 之上学习补偿性 delta action。之后让“base policy probing 加 specialist residual actor”组成的 hybrid controller 生成 demonstration trajectory，再把这些多任务轨迹 distill 回通用 VLA 权重。衔接机制是 base-policy probing：specialist 不是直接替代通用模型，而是只在 base policy 自然到达的状态分布上接管并生成对 generalist 最有价值的纠偏数据。参数上先学 residual RL，再用 SFT 或 LoRA 方式更新 VLA；质量控制依赖仅保留成功 rollout、probing horizon 设计与数据源对照。该文主要把 residual intervention 转成可内化数据，而非直接部署 specialist。 | 属于 Composite | §7.3 | 链式 | `P7→P5` | embodied / robotics | 若把 residual data generation 换成普通 rollout 收集后微调，论文核心的 distribution-aligned corrective data 机制即消失。其关键在于构造原本不存在的高价值训练集，因此归 §7.3。 |
| [He25g] | Scalable Data Synthesis for Computer Use Agents with Step-Level Filtering | 论文以 teacher CUA 在 OpenWebVoyager 和 Mind2Web 任务上的原始 rollout 为起点，先在 Chromium 环境收集 screenshot-action 序列，再由 o4-mini 事后补写 reasoning-rich step。接着对每个 step 用 grading model 生成 0 到 10 的质量分和 rationale，并据此形成仅对高分 step 计算损失的 masked SFT 数据。衔接机制是 step-level filtering：模型保留全轨迹上下文，但只让被判为高质量的 step 贡献梯度，不对低质 step 做 rewrite。训练上更新 student Qwen2.5-VL，另训练 StepRM 去蒸馏大模型 grader；vision tower 冻结。质量控制包含 trajectory-level success filter、step-level human validation 与 grader consistency 检验。 | 属于 Composite | §7.3 | 链式 | `P7→P7→P5` | GUI / mobile | 去掉 step-level grading，只用成功轨迹整体 SFT，性能明显下降，说明中间筛炼机制是方法核心。这里操作的是“哪些 step 进入损失”的经验集合分布，而非改写 step 内容，因此归 §7.3。 |
| [Son24] | Trial and Error: Exploration-Based Trajectory Optimization for LLM Agents | 论文先用专家成功轨迹对 agent 做 SFT，随后让当前 policy 在 WebShop、ScienceWorld、ALFWorld 中探索，收集失败或低回报 trajectory。然后把这些探索失败与固定 expert success 配成对照样本，用 Bradley-Terry 视角下的 DPO 直接优化 policy。衔接机制是 failure-success contrastive pairing：方法不是简单把成功探索加回训练集，而是显式利用失败与成功之间的 preference signal。训练只更新 policy，reference policy 在每轮 DPO 中固定；quality control 则通过 reward-delta 筛选，仅保留“探索失败对专家成功”这类有效 pair。新 policy 再进入下一轮探索。 | 属于 Composite | §7.3 | 闭环式 | `P7→P5↺` | general | 若把中间环节换成普通 self-training 或 rejection-sampling FT，论文最关键的 failure-to-preference 机制消失。作者也直接拿 RFT 对照，证明仅靠成功轨迹扩充不够。 |
| [Fai26] | AutoSurfer -- Teaching Web Agents through Comprehensive Surfing, Learning, and Modeling | 论文从 WebArena 站点开始做系统化 BFS surfing，先在页面层面发现 simple task，再基于探索路径组合出更复杂的 explored task tuple。随后用 web agent 在真实网站上对这些 task tuple 做 refinement，补全更真实的轨迹和任务描述，再把结果序列化成 ShareGPT 风格 SFT 数据去训练网站专属小模型。衔接机制是 exploration-grounded task synthesis 加 refinement：探索轨迹先定义任务，再作为 hint 驱动代理把粗糙任务还原成可学习的完整轨迹。训练对象是小型 web model，GPT-4.1 负责 surfing 与 refinement 并保持冻结。质量控制包括 risky task 排除、1 到 5 分的 task scoring、失败 tuple 丢弃、30 step 上限和 deduplication。 | 属于 Composite | §7.3 | 链式 | `P7→P1→P5` | web | 若删去 exploration 与 refinement 的双阶段衔接，仅剩常规网页数据合成，AutoSurfer 的主要价值不成立。机制明显作用在任务与数据集构造层，因此归 §7.3。 |
| [Qi24] | WebRL: Training LLM Web Agents via Self-Evolving Online Curriculum Reinforcement Learning | 论文从 WebArena-Lite 的人类示范初始化 actor 与 critic，然后在每一轮把先前失败任务作为种子，由 GPT-4o 扩展出新的 curriculum instructions。当前 actor 在这些任务上 rollout，冻结的 outcome reward model 负责在缺少环境 reward 时给出 success signal，critic 则提供优势估计；成功轨迹会进入 replay buffer。衔接机制是 self-evolving curriculum 加 confidence-filtered replay：经验不是直接按原分布回放，而是通过失败驱动的任务扩展和 actor-confidence 区间筛选后再混入训练。参数更新上 actor 与 critic 持续更新，ORM 先离线训练后固定使用。质量控制包含 task feasibility filtering、critic-based task range filter、perplexity-based replay filtering 与 KL 约束。 | 属于 Composite | §7.3 | 闭环式 | `P7→P4→P6↺` | web | 若把 curriculum generation 和 replay filtering 换成普通在线 RL，WEBRL 的 headline 设计不成立。其 learned evaluator 并未被专门设计成随 policy 刷新，critic 也更接近标准 RL 组件，因此不归 §7.1，而归 §7.3。 |
| [Put24] | Agent Q: Advanced Reasoning and Learning for Autonomous AI Agents | 论文以 MCTS 搜索得到的成功与失败 interaction trajectory 为经验源，把每轮搜索树缓存进 replay buffer。随后依据树中节点的 Q 值与 AI critique score，把 sibling action 编译成 node-level preference pairs，再用 DPO 更新 agent policy。衔接机制是 search-to-preference compilation：搜索本身不是终点，树上的 value aggregation 被专门转成可内化的 preference supervision。训练时 policy 迭代更新，AI critic 保持冻结；同时用 value threshold 过滤过于接近的 pair。新策略再次进行 MCTS 并继续扩展经验库。 | 属于 Composite | §7.3 | 闭环式 | `P7→P5↺` | web | 把搜索树 pair compilation 换成普通 outcome-only SFT 或 DPO，论文主贡献就会弱很多。文中对 AI feedback 和 outcome-only 对照都支持其属于经验集合构造型 composite。 |
| [Bai24] | DigiRL: Training In-The-Wild Device-Control Agents with Autonomous Reinforcement Learning | 论文先以 AitW 离线人类演示做 offline RL warm-up，再让当前 device-control agent 在真实移动端环境里做大规模在线交互。在线阶段由 Gemini 1.5 Pro 只看 screenshot 做二值 AutoEval，再结合 instruction-level value function 与 doubly-robust step-level advantage 过滤出最值得学习的任务与动作。衔接机制是 autonomous feedback plus two-tier hard filtering：环境交互不会直接整批喂给策略，而是先经过任务层 curriculum 与 step 层 advantage 筛炼。训练更新 actor 与两个 value function，视觉编码器与 instruction encoder 冻结。质量控制主要是 parallel rollout、binary success judging、instruction difficulty prioritization 与 step-level thresholding。 | 属于 Composite | §7.3 | 闭环式 | `P7→P5↺` | GUI / mobile | 若把 AutoEval 和双层过滤换成普通在线 RL，DigiRL 的主要方法学贡献会消失。其核心作用于“哪些在线经验被保留并以何种粒度进入更新”，故更接近 §7.3；但它也带有较强 RL system 色彩。 |
| [Das25] | BLAZER: Bootstrapping LLM-based Manipulation Agents with Zero-Shot Data Generation | 论文从大模型根据任务描述与模拟器真值状态零样本生成的 Python control plan 出发，在 CoppeliaSim 中执行这些计划并由 simulator verifier 检查是否完成任务。成功计划被聚合成任务数据库，再作为监督样本去 LoRA 微调较小的 manipulation model。衔接机制是 simulator-in-the-loop verification：只有可执行且成功的合成程序才能转化为最终训练数据。训练对象仅是学生 policy，teacher LLM、模拟器校验器与运动规划器保持固定。质量控制完全依赖执行验证与失败样本剔除，另有样本规模 ablation。 | 属于 Composite | §7.3 | 链式 | `P7→P5` | embodied / robotics | 若用普通数据收集替代“生成代码再执行验证”，BLAZER 的核心就不存在。这里关键不是单纯 teacher-student，而是生成、执行、验证三步联动构造经验集，因此归 §7.3。 |
| [Ge26] | Internalizing Agency from Reflective Experience | 论文以 agent 在 CodeContests、WebShop、ALFWorld、ScienceWorld、Sokoban 中生成的 interaction history 为经验源，先通过 periodic reflection 与 rollback 构造一棵 rollback tree。每次反思会定位 rollback target，并生成一个自然语言 experience summary 来指导从该点重新分支，得到改进后的 trajectory。随后把这些“有 summary 才能成功”的改进行为转成 counterfactual distillation 样本，训练 policy 在没有 summary 的情况下直接输出 corrected action，同时再配合 rehearsal 保留原有能力。衔接机制是 reflective backtracking 加 experience-to-policy distillation，而不是简单把反思文本拼进推理时上下文。质量控制依赖 improved-branch 选择与 rehearsal subset。 | 属于 Composite | §7.2 | 链式(含局部迭代) | `P7→P1→P5` | general | 若去掉 rollback reflection 和 counterfactual distillation，只保留普通 self-training，LEAFE 不再成立。其关键机制明确改写单条经验附近的内容并把修复后的动作内化进权重，符合 §7.2；但前段含 rollback tree 的局部迭代。 |
| [Xio24] | Watch Every Step! LLM Agent Learning via Iterative Step-level Process Refinement | 论文从 expert success trajectory 出发，先用当前 agent 作为 scorer，通过 Monte Carlo continuation 估计 expert step 与 agent-sampled alternative step 的 step reward。随后基于 reward margin 构造 contrastive step pair，并把这些 pair 与 outcome-level supervision 一起用于 mixture trajectory optimization。衔接机制是 step-level process refinement：模型并不改写 step 内容，而是从 expert prefix 上生成替代动作、估计其未来回报，再把“好 step 胜过坏 step”的局部偏好编译成训练信号。训练对象始终是 policy，自身 scorer 在轮次之间被新 policy 替换。质量控制依赖 reward threshold、outcome consistency 与迭代轮次控制。 | 属于 Composite | §7.3 | 闭环式 | `P7→P5↺` | general | 中间机制若替换成普通 expert SFT 或 outcome-only DPO，IPR 的核心创新即消失。它作用在 step-pair 的构造与筛选上，不改写轨迹内容，因此更应放在 §7.3 而非 §7.2。 |
| [Yua25c] | Agent-R: Training Language Model Agents to Reflect via Iterative Self-Training | 论文从 WebShop、SciWorld、TextCraft 中当前 actor 的 MCTS 搜索树出发，区分 bad trajectory 与 good trajectory。随后由 actor 自己定位 first error step，把 bad prefix 截断，再与 good suffix 拼接，并插入 revision signal，形成 revision trajectory。衔接机制是 transition-point determination：不是整条失败轨迹统一事后纠正，而是定位可修复的最早错误点并在该点接入正确后续。之后把 good trajectory 与 revision trajectory 一起用于 SFT 更新 actor，并在下一轮重新收集新 revision 数据。质量控制依赖 reward threshold、alpha annealing 与 only-high-quality revision acceptance。 | 属于 Composite | §7.2 | 闭环式 | `P7→P1→P5↺` | general | 若把中间的 error localization 和 splice revision 换成常规收集后微调，Agent-R 就退化为普通 self-training。其核心显然是改写单条 trajectory 的内容与监督形式，因此归 §7.2。 |
| [Wan26al] | Skill-SD: Skill-Conditioned Self-Distillation for Multi-turn LLM Agents | 论文从 student 在 AppWorld、Sokoban 等多轮环境中的 on-policy trajectory 出发，先由固定 summarizer 把整条 rollout 抽象成 skill JSON，包含 success_analysis、mistake_analysis 与 golden_workflow。接着系统把 skill 存入 buffer，用 UCB 机制为每个任务选择最有效 skill，并只把 skill 注入 teacher prompt，而 student 仍在无 skill 的原始 prompt 下行动。衔接机制是 asymmetric skill-conditioned self-distillation：teacher 看见 skill 后产生更好分布，student 再通过 importance-corrected reverse-KL 把这种改进内化进参数，避免测试时依赖检索。训练中 student 结合 GRPO 与 SDL 联合优化，teacher 与 student 动态同步，summarizer 固定。质量控制依赖 UCB skill retrieval、direct skill injection baseline 与 frozen teacher 对照。 | 属于 Composite | §7.2 | 闭环式 | `P1→P5↺` | general | 若移除 skill extraction 和 teacher-only conditioning，只剩普通 on-policy RL 或直接 skill prompt 注入，文中显示会更差。这里中间机制先改写单条经验为可复用 skill，再做内化，典型是 §7.2。 |
| [Ye26f] | Online Experiential Learning for Language Models | 论文以用户侧部署期间积累的多轮 textual interaction trajectory 为经验源，先由 extractor 从原始日志中抽取 transferable experiential knowledge，如规则、策略或失败教训。随后把这些 textual knowledge 只提供给冻结 teacher，student 则在无该知识上下文的条件下通过 on-policy reverse-KL context distillation 去拟合 teacher 分布，从而把外部经验压入参数。衔接机制不是直接拿 raw trajectory 训练，而是先做 experience extraction，再做 context-to-weights consolidation。每轮部署后，更新后的模型继续收集新轨迹并抽取更高级的经验项。质量控制主要依赖结构化 experience item 格式、跨 episode 累积和 raw trajectory 对照。 | 属于 Composite | §7.2 | 闭环式 | `P1→P5↺` | general | 论文明确比较 raw trajectory 与 extracted knowledge，前者明显更差，因此中间抽取步骤不可替代。该机制是对单条或小批经验内容的抽象改写并内化，符合 §7.2。 |
| [Wan25x] | STeCa: Step-level Trajectory Calibration for LLM Agent Learning | 论文先用 expert trajectory 对 base agent 做 SFT，再让其在 VirtualHome、ALFWorld 中探索，借助 Monte Carlo continuation 识别 deviated action。随后用外部 LLM 把偏离步改写为带 reflective thought 的 corrected action，并与后续 expert suffix 组装成 calibrated trajectory。衔接机制是 step-level calibration：它并不只是给 step 打分，而是明确重写错误位置的内容并把修复后的局部子轨迹作为新监督。最终把 successful、deviated 与 calibrated trajectory 混合训练 agent。质量控制依赖 deviation threshold、MC reward estimation 与 reflection-quality 对照。 | 属于 Composite | §7.2 | 链式 | `P1→P5` | embodied / robotics | 去掉 calibration，只用 expert 加探索数据训练，性能会下降；而且 self-generated reflection 明显弱于 GPT-4o reflection。关键机制作用于单条 trajectory 内容重写，故归 §7.2。 |
| [Wu25b] | GUI-Reflection: Empowering Multimodal GUI Models with Self-Reflection Behavior | 论文以现有成功 GUI 轨迹与程序化 task template 为起点，先构造 verification、reversal、reattempt 三类 reflection-oriented 预训练与离线 SFT 数据。在线阶段，当前 GUI model 在模板环境中交互，成功轨迹做 step-wise filtering，失败轨迹则由 verifier 定位 error step，并据此挖掘 pre-error 与 post-error correction 样本。衔接机制是把失败执行转成结构化 reflection behavior supervision，而不是单纯把成功轨迹继续堆叠。训练对象始终是 InternVL2.5-8B GUI model，verifier 与模板环境固定。质量控制依赖 Gemini 过滤、错误步定位、课程式 task sampling 与 success-threshold 解锁更难任务。 | 属于 Composite | §7.2 | 闭环式 | `P1→P5↺` | GUI / mobile | 若只用无 reflection 的 SFT，复杂任务明显更差，说明中间的 reflection data construction 不可省略。虽然含在线 curriculum，但主贡献作用在失败经验内容的修复与再表述，因此归 §7.2。 |
| [Yan24m] | ReAct Meets ActRe: When Language Agents Enjoy Training Data Autonomy | 论文先用 1-shot ReAct prompting 收集初始成功 trajectory 训练 base model，然后让当前 agent 在 AlfWorld 与 WebShop 中自主探索。对探索到的外部动作，固定的 ActRe annotator 会事后补写 posterior rationale，把单纯 action 变成 reason-then-act 样本；环境终局 reward 则为这些自动拼装的新轨迹提供质量标签。衔接机制是 posterior annotation：它把本来没有解释的探索动作转写成 ReAct-style supervisory trajectory，再配合成功与失败数据做 contrastive self-training。训练对象是 agent policy，ActRe annotator 不训练。质量控制依赖二值奖励、初始轮仅保留成功数据以及高回报过滤。 | 属于 Composite | §7.2 | 闭环式 | `P7→P1→P5↺` | general | 把 ActRe posterior rationale 补写拿掉，方法就只剩普通探索加再训练。中间机制显然在改写单条经验内容，并非单纯筛选数据，因此归 §7.2。 |
| [Xu26j] | CLEANER: Self-Purified Trajectories Boost Agentic Reinforcement Learning | 论文从 code-action-observation 组成的 noisy tool-use trajectory 出发，在执行错误触发后先做 lookahead correction，生成可能成功的替代 reasoning 与 code。随后通过 Similarity-Aware Adaptive Rollback 判断是仅替换代码，还是连同 reasoning 一起深度替换，把原失败轨迹改写成 self-purified trajectory。衔接机制是 SAAR：它不是过滤坏数据，而是按语义相似度选择 shallow 或 deep rewrite，再把纯化后的轨迹送入 GRPO。训练只更新 policy，环境不变；同时用成功执行验证、最多 K 次 retry 与 purified-raw curriculum mixing 控制噪声。文中还检验了关闭 SAAR 推理后性能基本保留。 | 属于 Composite | §7.2 | 闭环式 | `P1→P5↺` | code | 若把 SAAR 换成普通 tool-RL，论文的主增益消失；且“关闭 SAAR 仍保留大部分收益”说明 purified trajectory 已被内化。因为机制实质上重写单条轨迹内容，故归 §7.2。 |
| [Din26] | AgentHER: Hindsight Experience Replay for LLM Agent Trajectory Relabeling | 论文以 WebArena 和 ToolBench 中的大量 failed trajectory 为经验源，先做 failure type 检测与 severity weighting，丢弃不可恢复的失败。接着抽取 trajectory 实际达成的 outcome，再让 LLM 反推一个该轨迹确实满足的新 goal，最后把 relabeled goal-trajectory pair 打包成 SFT、DPO 或 ShareGPT 样本。衔接机制是 goal-conditioned hindsight relabeling：轨迹内容本身保持不变，但任务目标被重写成与实际结果一致的自然语言目标。训练只更新下游 agent，原失败轨迹和 relabeling 系统固定；质量控制包含 confidence gating、多评审复核和 failure-severity reweighting。该 pipeline 可在新模型产生的新失败上继续重复。 | 属于 Composite | §7.2 | 链式(含局部迭代) | `P1→P5` | general | 若改成随机 relabel 或无 confidence filter，性能和标签噪声都会明显恶化，说明中间 relabeling 机制是核心。它改写的是单条经验对应的目标内容而非经验集合选择，因此更接近 §7.2。 |
| [Ala25] | Memento No More: Coaching AI Agents to Master Multiple Tasks via Hints Internalization | 论文从人类专家为 ToolQA、OfficeBench 失败代理编写的 hints 出发，先让 teacher 在 hints 条件下生成成功 trajectory。随后 student 在无 hint 或高比例 hint dropout 的条件下，用前向 KL distillation 内化 teacher 行为；后续轮次中 Reviewer 检测新的错误类型，人类再给 targeted corrective hints，继续做下一轮 internalization。衔接机制是 hints-to-weights distillation：hint 不长期留在测试 prompt，而是被逐轮压入 LoRA adapter。训练中只更新 adapter，base model 冻结。质量控制依赖 reviewer、mistake-type balancing、旧成功样本回放与 dropout rate ablation。 | 属于 Composite | §7.2 | 闭环式 | `P1→P5↺` | tool-use | 若把中间的 hint-conditioned teacher distillation 换成普通多任务提示或普通微调，MNM 的主结果不再成立。机制显然作用于单条任务经验内容的补写与内化，属于 §7.2。 |
| [Zho24e] | Policy Improvement using Language Feedback Models | 论文先从 grounded instruction-following agent 的 rollout 出发，把视觉或状态观测 verbalize 成语言描述，再让 GPT-4 对每个 action 生成“productive 还是 not desirable”的语言反馈。随后这些反馈被蒸馏成小型 Language Feedback Model，LFM 再去过滤新一轮 policy rollout，只把被判为 desirable 的 action 纳入 imitation dataset。衔接机制是 filtered imitation learning：反馈模型不直接做 RL reward，而是先把轨迹裁剪成可学习的 desirable behavior 子集，再更新 policy。训练中 LFM 与 policy 都会被训练，teacher LLM、verbalizer 与环境保持固定。质量控制包括 balanced productive/non-productive sampling 与多轮 dataset 累积。 | 属于 Composite | §7.3 | 闭环式 | `P4→P7→P5↺` | general | 去掉 verbalized feedback 与 LFM filtering，方法就退化为普通 BC 或直接用大模型行动预测。这里关键是先把 evaluator 能力外化成 tokenized desirable-action 集合，再内化进 policy，因此归 §7.3。 |
| [Zha26] | RetroAgent: From Solving to Evolving via Retrospective Dual Intrinsic Feedback | 论文以 agent 在 ALFWorld、WebShop、Sokoban、MineSweeper 中的在线 trajectory 为经验源，episode 结束后由 reflection function 把整条轨迹转成一个三元反馈：scalar potential、binary success prediction 与自然语言 lesson。随后 scalar branch 被变成 intrinsic numerical reward 与环境奖励结合用于 GRPO，language branch 被写入 memory buffer，并通过 SimUtil-UCB 为后续任务检索 lesson。衔接机制是 retrospective dual intrinsic feedback：同一条经验同时被重编译成数值进展信号和文本教训，二者再共同塑造下一轮 policy。训练更新 decision policy，在 RL-trained 版本中还更新 reflection policy；sentence encoder 冻结。质量控制包括 historical best baseline、lesson relevance threshold 与 UCB 防止 memory collapse。 | 属于 Composite | 跨类 | 闭环式 | `P1→P5↺` | general | 若去掉 retrospective dual feedback，只剩普通 GRPO 或普通 memory retrieval，论文主机制即不存在。language lesson 分支更像 §7.2 的内容改写，numerical reward 与 retrieval 分布控制又带有 §7.3 特征，因此判跨类；同时包含 7 条 pathway 之外的 trajectory-to-intrinsic-reward 转化。 |
| [Sar24b] | VLM Agents Generate Their Own Memories: Distilling Experience into Embodied Programs of Thought | 论文从人类非专家演示、被动视频和 agent 自身探索得到的 noisy trajectory 出发，先由 VLM 进行 abstraction，纠正动作并生成包含因果关系、对象状态变化、子目标和视觉关键点的 multimodal programs of thought。随后在执行阶段通过 human-in-the-loop 反复验证与修正，只有最终可执行成功的 optimized example 才进入 memory。衔接机制是“trajectory abstraction 再成功验证”，其产物既可作为 RAG memory 在测试时检索，也可进一步做 SFT 让较小模型内化这些 multimodal examples。训练上固定强 VLM 做抽象，必要时训练 inverse dynamics 和下游 student。质量控制主要依赖执行成功验证、失败样本丢弃与 HITL retry 上限。 | 边界 | §7.2 | 链式(含局部迭代) | `P1→P5` | embodied / robotics | 若只看“trajectory abstraction 与 memory reuse”，它更接近 token-level distillation而非本节的 policy internalization；但论文确实包含把 distilled example 继续 SFT 到 student 的分支，因此不是完全排除。综合看其核心仍在 memory artifact，本节匹配度略弱，故判边界；内环 HITL 使其呈链式含局部迭代。 |