

| 引用标识 | 论文标题 | 关键思想与方法 | 粒度 | 形态 | 领域 | 备注 |
|---|---|---|---|---|---|---|
| [Bai22b] | Constitutional AI: Harmlessness from AI Feedback | 论文分两段把 AI evaluator 的能力转进 policy：SL-CAI 中，反馈模型先按 constitution 生成 critique 与 revision，policy 再对 revision 做 SFT；RL-CAI 中，AI judge 对完整回答给 preference，训练出 preference model 后再给 policy 提供 reward 做 RL。真正进入 policy 训练的产物因此同时包含生成式 revision 和判别式 preference reward。能力提升主要体现在 harmlessness 与 non-evasive safety，同时尽量保留 helpfulness。方法带有显式迭代修订，但不是持续共演化的 actor-critic。 | outcome | 判别式+生成式 | safety / alignment | 同时包含 SL-CAI 的生成式 revision 与 RL-CAI 的判别式 preference reward；两部分都更新 policy。 |
| [Lee23b] | RLAIF vs. RLHF: Scaling Reinforcement Learning from Human Feedback with AI Feedback | 该文用 off-the-shelf LLM 充当 evaluator，对两条完整回答给 pairwise preference，或直接对单条完整回答给 scalar score。canonical RLAIF 先用这些 AI preferences 训练 reward model，再用 REINFORCE with baseline 更新 policy；d-RLAIF 则直接把 judge score 当在线 reward。进入 policy 的核心 feedback artifact 是 response-level 的偏好或分数，而不是自然语言 critique。提升主要体现在 summarization、helpfulness 与 harmlessness 等 alignment 任务，并讨论了 iterative RLAIF 以缓解 RM staleness。 | outcome | 判别式 | safety / alignment |  |
| [Guo24] | Direct Language Model Alignment from Online AI Feedback | evaluator 是在线 LLM annotator，它在每次迭代里对当前 policy 采样出的两条完整回答给出 pairwise preference。该偏好不再先蒸馏成单独 RM，而是直接进入 DPO、IPO 或 SLiC 这类 direct alignment loss 来更新 policy 参数。feedback artifact 是 response-pair 级别的 chosen versus rejected 标签，因此属于 outcome-level 的判别式反馈。改进面向 TL;DR、helpfulness、harmlessness 等通用 alignment 任务，关键创新是 on-policy 在线采样与在线 AI 标注闭环。 | outcome | 判别式 | safety / alignment |  |
| [Yua24] | Self-Rewarding Language Models | 同一个 LM 同时扮演 policy 与 evaluator，judge 先对完整回答生成简短 CoT justification 并给 1 到 5 分。真正进入 policy 训练的是由分数构成的 winner-loser preference pairs，随后用 iterative DPO 更新模型，而不是把 critique 文本直接当监督目标。因而 policy 消费的是 outcome-level 的判别式反馈，尽管 judge 在前端会生成解释。该方法通过多轮 self-rewarding 让模型同时提升 instruction following 与 judge ability，是显式的自举式迭代改进。 | outcome | 判别式 | dialogue / chat |  |
| [Don23] | RAFT: Reward rAnked FineTuning for Generative Foundation Model Alignment | evaluator 是训练好的 reward model，它对同一 prompt 下采样得到的多条完整回答打分并选出最高分样本。policy 不走 PPO，而是把被 RM 选中的 best-of-K 回答当成伪金标，直接做 SFT；可选地还把 KL 惩罚并入 ranking reward。进入训练的是被 scalar reward 筛出的整条 response，因此是 outcome-level 的判别式 evaluator-to-policy transfer。提升主要体现在 helpful-harmless alignment，同时以更低算力代替传统 RLHF。 | outcome | 判别式 | safety / alignment |  |
| [Ses24] | BOND: Aligning LLMs with Best-of-N Distillation | evaluator 是 reward model，它定义了 Best-of-N 分布并决定哪些完整回答属于高奖励样本。policy 通过 forward KL 的 SFT 式 distillation 加上 backward KL 的 RL 式更新去逼近该分布，J-BOND 还用 EMA anchor 做迭代蒸馏。真正进入训练的是 sequence-level 的 best sample 与 rank-derived reward，而非解释文本，因此是 outcome-level 判别式反馈。性能提升来自把 expensive best-of-N test-time search 内化进单次采样的对齐后 policy。 | outcome | 判别式 | dialogue / chat |  |
| [Gui24] | BoNBoN Alignment for Large Language Models and the Sweetness of Best-of-n Sampling | evaluator 是 off-the-shelf reward model，它在每个 prompt 的 n 个完整回答中选出 best 与 worst。policy 通过 SFT-BoN 与 IPO-BoN 的组合目标，把 RM 定义的 best-of-n 偏好分布蒸馏到权重中，而不是在线查询 evaluator。进入 policy 的监督本质是 response-level 的极值排序对，因此属于 outcome-level 判别式反馈。收益主要体现在通用 alignment 中更好地逼近 best-of-n 分布，同时减小 off-target drift。 | outcome | 判别式 | dialogue / chat |  |
| [Wu24d] | Meta-Rewarding Language Models: Self-Improving Alignment with LLM-as-a-Meta-Judge | 同一模型分化出 actor、judge、meta-judge 三个角色，judge 先给完整回答打分，meta-judge 再对不同 judgments 做 pairwise battle 与 Elo 排序。policy 更新时真正消费的是由 judge score 选出的 response preference pairs，而 judge 自身也会用 meta-judge 生成的 judgment preference pairs 做 DPO。对 actor 来说，进入训练的仍是 outcome-level 判别式偏好，而不是 meta-judge 的解释文本。该方法通过多轮 self-improvement 让 response generation 与 judging 能力一起提升，是典型的自举式共演化框架。 | outcome | 判别式 | dialogue / chat |  |
| [Ack26] | Gradient Regularization Prevents Reward Hacking in Reinforcement Learning from Human Feedback and Verifiable Rewards | 论文中的 evaluator 可以是训练式 reward model，也可以是 LLM-as-a-judge 或 verifiable reward source，最终都被塌缩为 scalar proxy reward。policy 用 Dr.GRPO 这类 REINFORCE 风格 RL 目标更新，文章贡献在于额外加入 gradient regularization 或 reference reset 来抑制 reward hacking，而不是改 evaluator 本身。对 policy 真正起作用的是完整回答级别的 scalar reward，因此在 P6 视角下是 outcome-level 判别式。提升集中在更稳的 post-training 与更少的 overoptimization。 | outcome | 判别式 | safety / alignment | 只在采用 parametric RM 或 LLM judge 的子设置下是 clean P6；含有 verifiable reward 的子实验按你的口径并不都属于 P6。 |
| [Ren26b] | Reinforcement Learning from Meta-Evaluation: Aligning Language Models Without Ground-Truth Labels | evaluator 是回答 meta-questions 的 LLM judge，它对完整回答给出诸如“是否正确”的 YES 概率，再把这些概率加权汇总成 scalar reward。policy 端用 GRPO 或 CISPO 等 RL 目标，直接从该 reward 计算 advantage 并更新参数，不依赖 ground-truth label 或外部 verifier。因为进入训练的是 sequence-level 的标量回报，所以粒度是 outcome，形态是判别式。能力提升覆盖 GSM8K、faithfulness 与开放问答，且 evaluator 可以 frozen、self-eval 或 ensemble。 | outcome | 判别式 | reasoning (math) |  |
| [She24c] | Policy Filtration for RLHF to Mitigate Noise in Reward Models | evaluator 是标准 reward model，它对每条完整回答给 scalar reward。作者不改 PPO 主体，而是在采样后先按 RM 排名并做 Best-Random 或 Best-Worst 过滤，只把被选中的 responses 连同其 reward 放进 PPO buffer 更新 policy。policy 实际消费的是 trajectory-level 的 scalar reward 与筛选后的 response，所以属于 outcome-level 判别式反馈。改进主要在于缓解 noisy RM 带来的过拟合和训练不稳。 | outcome | 判别式 | dialogue / chat |  |
| [Wu25x] | Reward Model Routing in Alignment | 该文不是单一 evaluator，而是一个 router 在多个 reward models 之间为每个 query 动态挑选最合适的 evaluator。被选中的 RM 输出 pairwise preference 或 scalar reward，随后直接进入在线 DPO 管线更新 policy；router 自身再用负 DPO loss 做 Bayesian online update。对 policy 真正生效的是 response-level 的偏好判断，因此粒度是 outcome，形态是判别式。收益在于跨聊天与推理任务减少单一 RM 偏置并提升 alignment 稳定性。 | outcome | 判别式 | general |  |
| [Xu24d] | The Perfect Blend: Redefining RLHF with Mixture of Judges | evaluator 由 calibrated reward model、LLM-based judges 与 rule-based constraint judges 共同组成，输出的是是否满足约束的 binary judgment 与经校准的 reward。policy 可用 CRPG、CODPO 或 CRRAFT，把这些判断转成 masked reward、chosen-rejected pairs 或 reward-ranked SFT 数据来更新参数。进入训练的反馈仍是完整回答级别的判别式信号，因此粒度归为 outcome。性能提升覆盖 general chat、instruction following、math-code reasoning、engagement 与 safety，多 judge 结构用来抑制 reward hacking。 | outcome | 判别式 | general | 混合了 parametric judges 与规则约束，按你的口径不是完全纯净的 P6。 |
| [Fis24] | Robust Preference Optimization through Reward Model Distillation | evaluator 是显式 reward model 或 RM ensemble，它先给 response pairs 提供 reward difference。policy 不直接做经典 RL，而是把这些差分蒸馏成自身的 implicit reward，用 L2 distillation loss 加 forward KL 正则离线更新参数。进入训练的是 sequence-level 的 reward differences，因此属于 outcome-level 判别式反馈。改进重点是让 preference optimization 在 label bias、distribution shift 与 RM uncertainty 下更稳健。 | outcome | 判别式 | dialogue / chat |  |
| [Luo25f] | DPO Learning with LLMs-Judge Signal for Computer Use Agents | evaluator 是 GPT-4o judge，它对 GUI 下一步动作候选给 0 到 100 的分数，并可附带 reason。真正进入 policy 训练的是由高低分样本构造的 preferred versus rejected step pairs，随后用 DPO 微调 UI-TARS-2B。由于 judge 评的是单步 GUI action，因此反馈粒度是 process；而 DPO 消费的是偏好标签而非 reason 文本，所以形态是判别式。改进集中在 OS-World 等 computer-use 任务上的本地部署 GUI agent 能力。 | process | 判别式 | GUI / computer-use |  |
| [Din25b] | FAPO: Flawed-Aware Policy Optimization for Efficient and Reliable Reasoning | evaluator 是 generative reward model，它能定位 first invalid reasoning step，并区分 fully correct 与 flawed-positive rollout。虽然 evaluator 内部做的是 process diagnosis，但进入 policy 优化的是对整条 rollout 加上的 penalty 或 modified reward，再配合 FAPO 的 advantage 设计更新 policy。按你的口径，policy 真正接收的是 trajectory-level 标量回报，因此应判为 outcome-level 判别式。能力提升主要体现在 math 与 code reasoning 中减少靠猜答案或跳步得到的伪正确。 | outcome | 判别式 | reasoning (math) | evaluator 具有 step-level 定位能力，但该信息主要被压缩成 rollout-level 惩罚后再训练 policy。 |
| [Zho25p] | SWEET-RL: Training Multi-Turn LLM Agents on Collaborative Reasoning Tasks | evaluator 是单独训练的 turn-wise critic，在 backend 任务上是 Llama-3.1-8B-Instruct，在 frontend 任务上是 Qwen2-VL-7B 变体。它对每个 turn 的 action 给 advantage score，再把同一状态下的候选动作分成 chosen 与 rejected，随后用 DPO 更新 actor。由于监督直接落在 turn-level action 上，粒度是 process，且进入训练的是排名与偏好对而非解释文本，所以形态是判别式。能力提升体现在多轮协作式 programming 与 frontend design 代理。 | process | 判别式 | code |  |
| [Che25v] | Stepwise Guided Policy Optimization: Coloring Your Incorrect Reasoning in GRPO | evaluator 是 step-wise judge，它对完整 reasoning 做 post-hoc first-error identification，并据此计算一条 reasoning trajectory 的 partial-correctness reward。policy 端保持 GRPO 框架不变，只是把原本全对全错的 outcome reward 替换为基于 RTS 的 graded scalar reward 来做组内 advantage 计算。虽然 judge 使用了 step 信息，但进入 policy 的仍是每条完整回答一个标量，因此按你的定义应归 outcome-level 判别式。提升主要来自让 all-negative groups 也能产生有效梯度，强化数学推理训练。 | outcome | 判别式 | reasoning (math) | step-level 诊断被压缩成 trajectory-level 标量 reward 后再进入 GRPO。 |
| [Cha24b] | Dense Reward for Free in Reinforcement Learning from Human Feedback | evaluator 仍是标准 reward model，但作者把它最后层 attention 中对各 token 的归因拿出来，把终局 scalar reward 重新分配成 token-level dense rewards。policy 随后在 PPO 等 RLHF 管线中对每个 token 接收即时 reward，而不是只在序列末端吃到一分。由于 evaluator 信号直接作用在中间 token 动作上，粒度应判为 process，且反馈本质是数值型 dense reward，所以形态是判别式。收益体现在 sentiment、summarization 与 dialogue 等 RLHF 任务上的更好 credit assignment。 | process | 判别式 | dialogue / chat |  |
| [Wan26ac] | Enhancing LLM-based Search Agents via Contribution Weighted Group Relative Policy Optimization | evaluator 是 rubric-based LLM judge，它对每一轮 search 给 retrieval utility 与 reasoning correctness 两个 binary 信号，再合成为 contribution weight。policy 并不直接用这些文本分析，而是把 trajectory-level 的 outcome advantage 按 round 重新分配，得到 round-level advantages 后用 CW-GRPO 更新参数。由于训练信号已经细化到每一轮 search step，粒度是 process；反馈是二值和归一化权重，因此形态是判别式。改进主要针对多跳与知识密集型 search agents。 | process | 判别式 | search |  |
| [Xu25h] | Hybrid Reward Normalization for Process-supervised Non-verifiable Agentic Tasks | evaluator 包含 outcome reward 与 principle-based process reward model 两部分，其中 PPRM 会对每个中间 turn 输出分析与分数。policy 训练时真正消费的是经 ReNorm 规范化后的 step rewards 以及末端 outcome reward，再用 PPO 加 GAE 更新参数。因为 step-level 与 terminal-level 两类信号都直接进入 policy，粒度应标为 outcome+process；但进入优化的是标量分数而非分析文本，所以形态是判别式。提升集中在 non-verifiable agentic search 任务上更稳的多轮 credit assignment。 | outcome+process | 判别式 | search | 同时使用末端 ORM 与中间 PPRM 奖励。 |
| [Wan26s] | Building Autonomous GUI Navigation via Agentic-Q Estimation and Step-Wise Policy Optimization | evaluator 是单独训练的 Agentic-Q model，它对 GUI 状态动作对预测成功概率，并把这种 step-wise value 当作 policy 训练时的局部监督。policy 在 SWPO 阶段结合这些 Q-style 标量、group weighting 与 sample filtering，用 GRPO、RLOO 或 REINFORCE++ 风格目标更新参数。由于 supervision 直接落在 state-action segments 上，粒度是 process，形态是判别式。收益主要体现在 screen grounding 与 GUI navigation 的 sample efficiency 和 success rate。 | process | 判别式 | GUI / computer-use | 论文显式包含单独的 evaluator 构建阶段，介于 P4 与 P6 之间，但后半段确实把 evaluator 信号转进了 policy。 |
| [Li26p] | OS-Themis: A Scalable Critic Framework for Generalist GUI Rewards | OS-Themis 内部有 milestone-level verifier 与 trajectory-level judge，前者产出局部 binary verification 与 grounded feedback，后者给整条轨迹最终 binary reward。真正用于在线 RL 更新 policy 的主信号是 trajectory-level reward，而在自训练分支中则用 evaluator 过滤成功轨迹再做 SFT。按进入 policy 的主监督看，更接近 outcome-level；且 policy 消费的是 reward 或过滤结果而非文本解释，所以形态是判别式。能力提升覆盖 Android、桌面与 Web 的 generalist GUI agents。 | outcome | 判别式 | GUI / computer-use | critic 内部有 milestone 诊断，但 policy 更新主要依赖 final reward 与 trajectory filtering；边界上也可被看作 evaluator paper。 |
| [Din26e] | AdaRubric: Task-Adaptive Rubrics for LLM Agent Evaluation | evaluator 会先为任务生成 rubric，再对每个 step、每个维度输出 1 到 5 分和 confidence，并可伴随 rationale。policy 更新有两路：一是把聚合后的 trajectory scores 排名生成 chosen-rejected pairs 做 DPO，二是把更细粒度的 scores 直接当 reward 做 PPO。因而进入 policy 的监督同时包含 trajectory-level 与 step-level 数值信号，粒度应为 outcome+process；rationale 主要用于解释，不是训练主载体，所以形态是判别式。收益覆盖 Web、ToolBench、OSWorld、SWE-bench 等多类 agent 任务。 | outcome+process | 判别式 | general | 同时用于 step-level reward 和 trajectory-level preference construction。 |
| [Zha26am] | SRR-Judge: Step-Level Rating and Refinement for Enhancing Search-Integrated Reasoning in Search Agents | evaluator 是 generative SRR-Judge，它对每个 thought-action step 同时输出 explanation、1 到 5 rating，以及 refined thought 和 refined action。policy 训练用 iterative RFT，把 judge rate-and-refine 后得到的高质量轨迹作为对齐数据，同时还用 step ratings 做过滤与质量增强。因为 refined steps 直接作为训练目标，而 ratings 又参与筛选，监督粒度是 process，形态是判别式+生成式。改进集中在 deep-search 场景下的多轮 query 质量、推理连贯性与最终回答正确率。 | process | 判别式+生成式 | search | step rating 负责筛选，refined thought-action 负责生成式对齐，两者都参与训练。 |
| [Gon24b] | Training Agents with Weakly Supervised Feedback from Large Language Models | evaluator 是 critic LLM，它对整条 API-agent trajectory 给出质量评分，并从中挑出高分成功轨迹。policy 更新并非 RL，而是把 top p% trajectories 当作伪优质示范，配合通用 chat 数据做迭代式 SFT。由于进入训练的是被 trajectory-level score 选中的整条轨迹，粒度应为 outcome；policy 实际消费的是分数诱导出的筛选结果，而非自然语言解释，因此形态是判别式。能力提升主要体现在 API-bank 上的工具调用与多域 agent 执行。 | outcome | 判别式 | tool-use / API |  |
| [Hon25c] | Natural Language Actor-Critic: Scalable Off-Policy Learning in Language Space | evaluator 不是标量 critic，而是 generative language critic，它为每个 state-action 产出未来走向与为何该动作优劣的自然语言 critique。actor 先在 critique 条件下生成 refined action，再把 refined action 反向蒸馏回基础 policy，整个过程是 off-policy 的 language-space actor-critic。由于监督直接作用在中间 action 上，粒度是 process；而 policy 依赖的是 critique 文本与由其诱导的 refined action，所以形态应判为生成式。收益覆盖数学推理、20 Questions 对话策略与工具使用任务。 | process | 生成式 | general |  |
| [Wan26ad] | OpenClaw-RL: Train Any Agent Simply by Talking | evaluator 同时抽取两类 feedback：一类是 sequence-level 的 evaluative scalar reward，另一类是从用户话语或环境信号中抽出的 directive textual hints。policy 一方面用 binary RL 把标量回报送进 PPO 式目标，另一方面用 OPD 把 hint-enhanced teacher 与 student 的 log-prob gap 转成 token-level directional advantage。因而进入训练的监督既有 outcome-level 判别式回报，也有更细粒度的生成式语言反馈，整体应标为 outcome+process 与判别式+生成式。收益覆盖 terminal、GUI、SWE、tool-call 与 personalization 等广义 agent。 | outcome+process | 判别式+生成式 | general | 同时包含 sequence-level Binary RL 与 token-level OPD。 |
| [Yua25c] | Agent-R: Training Language Model Agents to Reflect via Iterative Self-Training | 论文用 actor 自身充当 verifier，在坏轨迹中定位 transition point，并插入 revision signal 后把好轨迹与修订轨迹一起做 SFT。虽然前端有 Good、Bad、Uncertain 的自评判断，但真正进入 policy 训练的是 revision trajectories 的文本 token，而不是 evaluator 的分数或偏好。按监督粒度看它更像 process-level 的生成式修订学习。性能提升体现在 WebShop、ScienceWorld 与 TextCraft 等多轮 agent 任务上，但从你的 taxonomy 看更接近 parametric-to-token supervision，而不是 clean P6。 | process | 生成式 | general | 可能不属于 P6；policy 更新主要是对 revision trajectories 做 SFT，更像 tokenized supervision。 |
| 未找到 | AgentRefine: Enhancing Agent Generalization through Refinement Tuning | 当前项目里我没有检索到这篇论文的条目或 PDF，因此无法按你的要求基于全文做判断。仅从题名看，它很可能也是把 refinement 结果转成新的训练轨迹来微调 agent policy。若真是如此，它可能更偏 process-level 的生成式 supervision，而不一定是 clean evaluator-to-policy。这里不做正式归类，避免猜测。 | 未明确 | 未明确 | general | 项目中未找到该论文，建议补充搜索或上传 PDF 后再补这一行。 |
| [Li26l] | No More Stale Feedback: Co-Evolving Critics for Open-World Agent Learning | evaluator 是与 actor 同步更新的 linguistic critic，它对完整 rollout 生成 score-aware 的自然语言 critique，再让 actor 基于 critique 产生 refined rollout。policy 用 GRPO 在 critique 条件下优化这些 refined trajectories，而 critic 也在同一批数据上用双轨 GRPO 共演化更新。对 actor 而言，进入训练的核心是 trajectory-level 的语言诊断与其带来的 refinement，因此粒度更接近 outcome，形态是生成式。提升覆盖 WebShop、ALFWorld、SciWorld 与 DeepSearch 等长程开放世界任务。 | outcome | 生成式 | general |  |
| [Pan24b] | Autonomous Evaluation and Refinement of Digital Agents | evaluator 可以是 end-to-end GPT-4V，也可以是 captioner 加 reasoner 的 modular evaluator，它会对轨迹给 success judgment，并在 device-control 分支做 per-step progress filtering。论文一部分只把 evaluator 用于 inference-time Reflexion，不属于 P6；但另一部分会把 evaluator 筛出的高质量 state-action pairs 用于 Filtered BC，从而更新 policy 权重。若只按会更新权重的分支看，进入训练的是 step filtering 结果而非解释文本，因此更接近 process-level 判别式。能力提升覆盖 WebArena、Android 与 iOS 数字代理。 | process | 判别式 | GUI / computer-use | 论文同时含 inference-time refinement 与 weight update；只有 Filtered BC 分支可算进 P6。 |
| [Zho24e] | Policy Improvement using Language Feedback Models | evaluator 是蒸馏自强 LLM 的 Language Feedback Model，它对每个动作判断该步是否 productive，并可扩展为解释式反馈。policy 训练时真正消费的是 LFM 标出的 desirable actions，再通过行为克隆把这些动作模式写入 policy 权重。由于 evaluator 逐步筛选 state-action pairs，粒度是 process；而主训练信号是 yes-no 生产性判断及其过滤结果，所以形态是判别式。能力提升体现在 ALFWorld、ScienceWorld 与 Touchdown 等 grounded instruction-following agent 任务。 | process | 判别式 | embodied | 解释式版本存在，但 policy 训练主用的是 binary desirability feedback。 |